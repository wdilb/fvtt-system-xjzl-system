# V14 ActiveEffect 专项开发计划

**本文件是 [`V14_PLAN.md`](V14_PLAN.md) 中 M2/S2.2–S2.11 与 M4 中 AE 子项的唯一开发和验收依据。**总计划只镜像里程碑与跨阶段依赖；V14 总体范围及公开 API 约束仍见 [`V14_UPGRADE.md`](V14_UPGRADE.md)。`[ ]` 表示代码尚未完成并验收。每项完成后在此记录 Foundry 构建号、场景、结果，再同步总计划；待决项取得结论前不得把相关工作标为完成。

## 目标与边界

1. 保留物品内嵌 AE：武学、内功、装备等仍可在自己的页面配置被动效果或可施加的效果。V14 允许 AE 作为合集包中的独立文档；**没有世界级 AE 集合**。现有通用状态继续以 `CONFIG.statusEffects` 定义，内容特有 AE 继续在武学、内功、物品等来源中定义，不批量复制到 AE 合集包。
2. 物品页面中可施加的 AE 可作为标准文档拖出；物品内嵌的 `transfer: true` 被动 AE 不可拖出。角色卡、Token 可接收物品和合集包中的可施加 AE。物品模板与合集包 AE 拖放后保留来源；Actor 内嵌 AE 拖放到另一 Actor 时，目标施加成功后删除来源。来源删除的权限与失败处理仍待核实（见 AE-08 补记）。`tiedToStance` AE 仅在双方当前架招相同时可施加。
3. 保留 `game.xjzl.api.effects` 的叠层、slug、来源、飘字、聊天卡片和 GM 权限语义；新增的“施加到 Actor”入口经过门面。可拖出的 AE 在物品之间拖放时是模板复制，不触发施加规则。
4. 以 V14 `duration`、`start` 和过期事件表达“持续 X 回合”；到期时清理且不重复结算，不强制规定核心 registry 与本系统回合脚本的先后。战斗外按 `CONFIG.time.roundTime = 2` 秒/回合计时。架招解绑继续由 `tiedToStance` 的业务清理负责；双方当前架招相同时允许施加此类 AE，交由目标架招的现有生命周期清理，不为它另改持续时间。
5. 状态选取器继续支持现有通用状态和场上物品 AE。V14 独立 AE 合集包可按核心能力打开、编辑和拖放，但本次不要求把所有 AE 搬入合集包，也不要求把 AE 合集包加入系统选取器。

## 已核对的 V14 契约

- [官方 Document 说明](https://foundryvtt.com/api/v14/modules/foundry.documents.html)：AE 可独立放在合集包，也可嵌入 Actor/Item；没有 `game.effects` 这样的世界集合。合集包中的 AE UUID 可用 `fromUuid` 解析。
- [官方源数据结构](https://foundryvtt.com/api/v14/interfaces/foundry.documents.types.EffectDurationData.html)：持久化 `duration` 是 `{value, units, expiry, expired}`；`remaining`/`secondsRemaining` 是准备后的派生数据，不写入源 JSON。开始锚点是 AE 顶层 `start`，不是旧版 `duration.startRound/startTurn/startTime`。`expiry` 存事件 ID，如 `"turnStart"`，不是中文显示名称。**达到时长且发生指定事件**才到期；`value: null, expiry: null` 才是真正无限。
- [官方事件表](https://foundryvtt.com/api/v14/variables/CONST.ACTIVE_EFFECT_EXPIRY_EVENTS.html)含 `turnStart`、`turnEnd`、`roundStart` 等；本机 14.368 源码的 `isExpiryEvent` 对 `turnStart` 限定为该 AE 所属 Actor 的当前 Combatant。角色无战斗参与者、战斗外计时以及多战斗切换仍须实机验收。
- [官方 registry API](https://foundryvtt.com/api/v14/classes/foundry.helpers.ActiveEffectRegistry.html)根据事件刷新并执行 `CONFIG.ActiveEffect.expiryAction`。本机 14.368 默认动作是 `"update"`（仅标记 `duration.expired`），不会物理删除；`"delete"` 才删除。核心战斗流程已在回合开始调用 registry。
- [ActorSheetV2](https://foundryvtt.com/api/v14/classes/foundry.applications.sheets.ActorSheetV2.html)和[ItemSheetV2](https://foundryvtt.com/api/v14/classes/foundry.applications.sheets.ItemSheetV2.html)内置 `_onDragStart`、`_onDropActiveEffect`，但默认落点会直接复制文档。本机 14.368 画布 Token 落点也直接创建 AE。这些默认路径不经过本系统 `addEffect`。
- 本机 14.368 的 `ActiveEffect#toCompendium` 默认清除 `origin` 与 `start`，适合把副本存为独立模板；`sourceName` 同步解析不了合集包内的 `origin` 时会显示 Unknown。拖放后要验来源 UUID 的合法性，也要验角色卡的来源名称回退显示。

建议使用下列源数据表达最常见的三回合效果：

```json
{ "duration": { "value": 3, "units": "rounds", "expiry": "turnStart", "expired": false } }
```

新建的模板不保存 `start`，施加到 Actor 时由 V14 初始化；从已生效 AE 拖放复制时保留其 `duration` 和 `start`，沿用当前剩余时间，不额外刷新或重置。若目标已存在同 slug AE，仍按现有 `addEffect` 叠层、延长或刷新规则处理。界面原有“0 代表无限”的输入语义须映射为 `value: null, expiry: null`，不能把 `0` 直接写为无限。`turns`、`seconds` 等少数旧数据按原业务意义逐条迁移，不能一律换算成 `rounds`。系统已设置 `CONFIG.time.roundTime = 2`。

V14 还增加 `showIcon`（状态图标显隐）、以 `token.` 为前缀的 Token 属性变更、变更应用阶段、`@` Actor 数据引用及 `subtract` 类型。**本次必须处理既有图标与数据格式的兼容**；Token 覆写和新类型本身按需使用，不为升级而重写现有业务脚本。详情见[官方 V14 发布说明](https://foundryvtt.com/releases/14.353)、[AE schema](https://foundryvtt.com/api/v14/classes/foundry.documents.ActiveEffect.html)和总纲 M0 机制结论。

| V14 改动 | 本系统需要配合的部分 |
|---|---|
| `showIcon: 0/1/2` | 核心默认 `1` 只给临时 AE 显示图标；`addEffect` 直接创建的永久通用状态不能只依赖默认值。逐项核对系统状态与装备被动图标，应常显的明确设 `2`；AE 编辑窗保留核心选项。 |
| `token.*` 变更 | 核心可把 `token.light`、视野等变化应用到 Token。本系统当前未定义此类 AE 变更，不为升级新建 Token 覆写数据模型；只检查现有 Token 自定义逻辑不被影响。核心暂不处理网格尺寸变更。 |
| `system.changes[].type/phase/value` | 旧数字 `mode` 要改字符串 `type`；默认 `initial` 早于系统派生计算，`final` 晚于派生计算。现有变更按目标字段核对阶段，叠层快照和编辑窗也同步新结构；`@` 引用和 `subtract` 只在原效果确有需求时采用。 |
| `origin` UUID | 核对 UUID 格式（包括 V14 允许的相对 UUID）及来源展示；来源文档暂时无法解析时不擅自改写合法值。复制时保留原合法来源，原值缺失才补源 Item/AE UUID。检查消耗品、架招同源豁免和旧世界脏数据。 |

## 设计决定与待验边界

| 主题 | 实施方案 | 需要验证的边界 |
|---|---|---|
| 拖放语义 | 可拖的 Item AE、Actor AE、独立 AE 使用 V14 文档拖放；目标端复制，源文档保留。只去掉与新父级冲突的文档 `_id`/位置字段；原有 `flags`、脚本、`system.changes`、`statuses`、`duration`、`start` 和合法 `origin` 原样保留。模板没有 `start` 时由核心在 Actor 上初始化；没有 `origin` 时才补源 Item/AE UUID。 | Actor 内嵌 Item、非关联 Token、合集包 UUID；已有时长的 AE 拖放后沿用剩余时间。来源名称需正确回退显示。 |
| 施加入口 | 复用 V14 `ActorSheetV2`/`ItemSheetV2` 的 `.draggable` + `data-effect-id` 与原生 `toDragData()`；角色卡和画布 Token 的接收改走 `ActiveEffectManager.addEffect`，复用叠层、权限和提示；拦截核心默认创建，保证一次拖放只施加一次。Item 卡接收可沿用核心模板复制。 | 角色卡已有 Item 拖拽监听、自定义 `_onDrop` 与核心 `.draggable` 的冲突；GM/玩家、合成 Actor、同 slug 重施加。画布 Token 默认直建，需在核心落点前接入门面。 |
| 被动效果 | **Item 内嵌、`transfer: true` 的被动 AE 不提供拖出**，包括装备页和 Actor 页展示的物品被动 AE；它留在 Item 上，继续按装备状态和破衣规则抑制。目标端也拒绝通过其他入口落入的此类拖放数据。可施加的 Item AE 允许拖出与施加。不能仅凭 `transfer: true` 拦截独立 AE，因为 V14 的该字段默认值也是 `true`。 | 逐项检查现有 Item AE 的 `transfer` 用法；其他被动来源若适用同一规则，一并禁止拖出。可施加模板的 Item→Item 复制仍可用。 |
| 时长到期 | `duration.value/units/expiry/expired` 与顶层 `start` 替换旧字段。优先采用核心 registry 的 `expiryAction: "delete"` 物理清理，再撤下系统的重复过期扫描；保留架招业务清理。既有回合脚本与核心到期处理的先后不作为产品约束，只要求结果正确、不会重复清理或抛错。 | 确认指定到期事件、目标 Actor、战斗外每 2 秒一回合、GM 与模块 AE；若全局 `expiryAction` 不适合世界中的其他效果，再改为核心 `"update"` 加单一系统清理。 |
| 架招绑定 | 保留 `flags.xjzl-system.tiedToStance`、`clearStanceTiedEffects` 和 `stopStance`。拖放绑定 AE 前，解析来源 Actor 与目标 Actor **当前已启用**的武学和架招；确认是同一架招才复制，并保留 flag、`origin`、`duration`、`start`，由目标架招现有的结束/切换逻辑清理。任一方未启用架招、无法取得来源 Actor/招式、或架招不同，直接结束施加，目标不创建 AE、源 AE 保留；不新增时长规则。 | `stanceItemId` 是各 Actor 本地 Item ID，招式 ID 也可能在重新录入后变化，不能直接跨角色比较。优先比较双方武学的相同合集包来源与架招名称；合集包来源不同或缺失时，按武学名称和架招名称比较。同武学重开、跨武学切换、主动解除、濒死解除仍按目标既有清理路径。 |
| AE 编辑窗 | 保留 V14 核心 details/duration/changes 页签，包括 `showIcon`、`transfer`、`statuses`、`origin` 和新变更格式；另加“高级规则配置”页签，保留 slug、可叠层、最大层数、架招绑定、脚本增删/启停/时机/代码及变更 key 自动补全。 | 切换页签、添加/删除脚本和保存时不能丢弃其他页签未保存字段；物品、Actor、独立合集包 AE 均能编辑。 |
| 状态来源 | `CONFIG.statusEffects` 继续提供通用状态与 TokenHUD；状态选取器继续保留现有分类、搜索、最近、收藏、场上物品特效、多目标和已生效管理。独立 AE 合集包可按 V14 原生能力作为额外模板来源，不强制接入该选取器。 | Item AE 数量随内容增长；不建立需与各 Item 同步的第二份状态目录。 |

### 实施时核对的边界

1. 核心 registry 与系统回合脚本的先后**不阻塞实施**；只做一轮正常到期、跳回合与不重复删除的联调。若 `expiryAction: "delete"` 的全局影响造成实际冲突，再走表中的回退方案。
2. 旧世界时长字段在 V14 清洗后可能无法完整恢复；迁移阶段用 V13 备份样本核对，无法还原的个例记录清楚，不阻塞 M2 的 AE 代码升级。
3. `tiedToStance` AE 的来源和目标 Actor 必须都正在使用同一架招；来源为 Actor 上的 AE 或其内嵌 Item AE 时按所属 Actor 取当前架招。独立 AE 或世界物品 AE 若无法确定来源 Actor，则本次不施加；来源或目标无活动架招、架招不同、身份无法唯一确认时也不施加。成功复制时保持原 `origin` 与剩余时长，由目标架招清理。

## 工作项与依赖

### A. 基线和核心数据链路

- [x] AE-01（S2.2；S2.1 已由 M0 结论关闭）审计 `XJZLActiveEffect` 的 `_preCreate`、`calculateChangesForStacks`、`isSuppressed`、`_displayScrollingStatus`。把快照及读写改为 `system.changes`，保留字符串 `type`，清理 `legacyTransferral` 过时注释，核对 `origin` 的合法 UUID。→ **已完成（14.368 实测）**：对照本机 V14 源码核实契约——`_displayScrollingStatus(enabled)` 签名不变，override 继续屏蔽核心飘字；核心 `isSuppressed` 现为 `system.isSuppressed ?? duration.expired`（基础模型无 isSuppressed 字段，默认即"过期则抑制"），叠加装备/破衣判定的 override 语义成立；创建数据到达 `_preCreate` 前已经过核心 `migrateData`（旧顶层 `changes` 迁入 `system.changes`、数字 `mode` 转字符串 `type`），`_preCreate` 快照改读 `data.system?.changes`；`legacyTransferral` 注释改为 V14 事实（内嵌 AE 恒留 Item 上，`transfer` 仅决定应用目标）；`calculateChangesForStacks` 回退分支已读 `this.system.changes` 无需再改。实测（testsystem 世界，临时 Actor 已清理）：V14 格式创建后 slug/stacks/scripts/baseChanges 全部正确（快照不含 prepareBaseData 补的 priority，干净）；`calculateChangesForStacks(3)` 按快照 5×3 乘算且保留字符串 `type`；旧格式入站（顶层 `changes`+`mode:2`）快照亦为 V14 结构；未装备 `transfer` 护甲特效 isSuppressed=true、装备后 false；创建/更新全程控制台无报错。origin：本文件无 origin 读写；V14 `origin` 为 `DocumentUUIDField(relative:true)` 自校验，非法值由核心迁移转 `flags.core.originText` 并置 null，业务侧 origin 写入/比较点归 AE-02/AE-06 核对。**备查（与 AE-02/AE-12 相关）**：核心 `migrateData` 对旧格式字符串 value 做 JSON.parse（如 `"10"`→数字 10），存量 V13 数据在加载清洗后 value 可能已是数字，本系统叠层乘算走 `Number()` 不受影响，世界迁移与门面归一化须知该清洗可能已发生；旧格式入站数据上 `"mode" in change` 命中的是核心非枚举兼容 shim（until:16），实际读取会触发弃用警告
- [x] AE-02（S2.3/S2.6）逐个迁移 `ActiveEffectManager`、`module/config.mjs`、`personality.mjs`、`item.mjs`、`actor.mjs`、各 Sheet、seeding 的 AE 构造和读取，并审计 `chat-manager.mjs` 破防的两处直接建 AE、Actor 内的 `toggleStatusEffect`、消耗品替换和性格自动 AE。门面入口归一化已到达的旧 `{changes, mode, icon, duration}` 普通数据，输出 V14 格式；不能把旧格式回写数据库，也不能用兼容层掩盖调用前的 `effect.changes` 读取错误。明确哪些核心直建入口有业务理由，迁移格式并保持其既有可见行为；其余施加入口走门面。叠层基准 `baseChanges` 随世界迁移处理。→ **代码完成并 API 实测（14.368，testsystem 临时数据已清理）**：①门面新增 `#normalizeEffectData`（icon→img、顶层 changes→system.changes、数字 mode→字符串 type、旧 duration→`{value,units,expiry}`；补丁对象的顶层 changes 整体替换底板已迁移数组；seconds 置 expiry:null、rounds/turns 默认 turnStart）；②manager 内部读写全迁 `system.changes`（叠层快照/乘算/减层/覆盖），`updateData` 用 `"system.changes"` 键；③`config.mjs` 62 个状态条目 `changes` 包入 `system`（`dead` 条目 changes 本为注释，62/62 实际包裹），逐条目补 `showIcon: 2`（见 AE-13）；④`actor.mjs` 破衣扫描、`item.mjs`/`personality.mjs` 性格 AE 构造与同步、`effect-interactions.mjs`/`effect-selection-dialog.mjs` 时长标签改走 manager `getDurationLabel`、6 个 Sheet 建特效 `icon:`→`img:`（V14 无此字段，残留会被核心清洗丢图标）、7 个 seed 文件 `img:` 兼容回退 + `system.changes` 包裹 + seed-origins 数字 `mode:2`→`type:"add"`；⑤审计结论：破防两处直建是展开 `CONFIG.statusEffects.pofang`，随条目迁移自动 V14（保留直建业务理由：不走叠层/飘字门面，行为不变）；核心 `toggleStatusEffect`（dead/dying/prone）走 `fromStatusEffect` 整体克隆条目并自动 `showIcon ??= ALWAYS`，无需改动；消耗品替换走 `toObject()`+门面自动 V14；character-wizard/customModifiers 的 `changes` 是系统数据非 AE。实测：旧格式入站经门面创建出的文档为完整 V14 结构（img/type/duration/slug）；一次性 3 层创建 stacks=3/数值 15/快照 5，再叠 2 层满层 5/数值 25，减层回 10；同单位延长 rounds 2+3=5 且 `start` 锚点保持不变；`getStatus`/性格 AE/常显传播均正常；全程控制台零错误零弃用警告。**注意**：调用方用扁平点号键 `"flags.xjzl-system": {...}`（而非嵌套 `flags:{}`）时 `getProperty` 读不到叠层标记，属 V13 以来既有行为，脚本录入规范不变。待验：战斗内颤手/心火/破防联调归 AE-05/AE-06 阶段。**审阅复核（第二轮）修正三项**：①seed 曾把旧数字 mode 原样包入 system.changes——核心 `migrateData` 只在迁移顶层 changes 时转换 mode，`mode:5` 会按默认 add 处理丢失覆盖语义（blade.json 实有 mode:5），新增 `module/utils/seeding/effect-data.mjs#normalizeLegacyChanges` 并接入 6 个 seed 文件（node 直测 mode:5→override、mode:2→add、新格式直通、空入参→[]）；②`#normalizeDuration` 曾在 value 为数值时提前返回，补丁与 V14 底板（如 pofang 的 `{value:1}`）深合并后旧单位键被忽略，现旧单位键存在时按其覆盖 value/units；③同单位延长分支曾对 `旧value+null` 求和（JS 中 5+null=5）并以 isDurationExtended 跳过刷新，限时效果无法被补丁改为无限，现要求两侧均为有限数值才延长。复验（14.368 实测）：pofang 底板+`{rounds:2}` 补丁→`{value:2,units:"rounds"}` 且真实可枚举键干净；限时→无限补丁生效；无限效果再施加有限时长不被缩短。**审阅复核（第三轮）修正**：seed-consumables 调用了 `normalizeLegacyChanges` 却缺 import（文件头无标记注释致上轮 sed 只插入调用未插入 import；`node --check` 查不出未定义引用，运行 seedConsumables 将在清空旧合集后报错）——已补 import，六个 seed 文件逐一做"调用/导入"配对检查，并以 node 直接 import seed-consumables 模块验证可加载
- [x] AE-03（S2.9）提供并验证 `game.xjzl.api.effects.getStatus(id)`：同步查询、未命中返回 `undefined`、命中返回可安全修改的 V14 格式副本；接口说明同步到 `SCRIPT_ENGINE.md`。这是 S4.2/S4.7 状态脚本迁移的前置。→ **已完成（14.368 实测）**：`ActiveEffectManager` 新增静态 `getStatus(id)`（`CONFIG.statusEffects[id]` 深拷贝，未命中返回 `undefined`），经 `game.xjzl.api.effects` 自动暴露；实测命中返回 V14 结构副本（id/name/img/showIcon/flags/system.changes，type 为字符串）、修改副本不污染 `CONFIG.statusEffects`、未命中返回 `undefined`。`SCRIPT_ENGINE.md` 已新增「查询状态定义」小节并注明脚本内不要直查 `CONFIG.statusEffects`
- [x] AE-12（S2.7）建立 AE 世界迁移框架：独立 world setting 记录已完成版本；遍历 Actor/Item AE、世界物品和非关联 Token 合成 Actor；对旧结构与 `baseChanges` 做可重试转换，仅全部成功后推进版本。**实现要求（2026-09-25 用户确认）：迁移只执行一次——版本号达标即整体跳过、零常驻开销；失败不标记完成、留下次重试。迁移范围分阶段扩展时（M2 的数据格式转换 → M4/AE-14 补充脚本等转换项）须采用分阶段版本标记（或单版本多阶段清单），防止先执行阶段把版本推进到"已完成"后，后扩展的转换项被跳过。**核心 WorldMigration 迁移框架能否承载本系统的完成版本记录与失败重试尚属调查项（未验证），调查结论须与总纲 Q1 决策（独立 world setting）统一表述后再定实现载体。V13 备份样本和实际世界迁移验收归 AE-14，不阻塞 M2。→ **M2 范围完成（2026-09-25，14.368 实测；真实 V13 世界副本的端到端验收归 AE-14）**。**调查结论**：V13/V14 核心均无 `WorldMigration` 类（两代客户端源码树与打包产物全量检索，仅剩 `SETUP.WorldMigration*` 本地化键残留），核心无法承载完成版本记录与失败重试；按总纲 Q1 决策以独立 world setting `xjzl-system.aeMigrationVersion`（Number，config:false，default 0）落地。**实现**：`module/migration/ae-migration.mjs`（阶段表 `AE_MIGRATION_STAGES` 按版本升序 + init 注册 setting + ready 仅 activeGM 执行（在线 GM 中 ID 最小者，避免多 GM 重复迁移）；版本达标时只有一次数字比较的开销；失败不推进版本号、下次启动自动重试并通知）；`module/utils/effect-conversion.mjs` 为转换规则唯一事实源（纯函数、无 foundry 依赖，AE-10 源 JSON 离线迁移复用同一套规则）。**阶段 1 范围与转换内容**：世界 Actor 的 AE 与其内嵌 Item 的 AE、世界 Item 的 AE、非关联 Token 的 delta.effects 与 delta.items 内嵌 AE（2026-09-25 二次审阅补充）；逐条读 `_source` 计算更新差异、有变化才写回（`_source` 为核心加载清洗后的形态，见下方数据边界实证）。转换规则：`label→name` 兜底、`icon→img`、顶层 `changes→system.changes`、数字 `mode→type`（与核心 #MODES_TO_TYPES 一致，mode:5→override 语义保留）、旧 duration 单位键处理 + 旧锚点核对、`baseChanges` 同步转换、非被动（transfer:false）且 `showIcon` 为 1/缺失的补设 2（AE-13 决策②，显式 0 保留）。**数据边界实证（2026-09-25 二次审阅修正，fromSource 旧数据实测）**：迁移读到的文档 `_source` 是核心加载清洗后的形态而非数据库原始记录——顶层 `changes`/数字 `mode`/`icon`/`label`/旧锚点大多已被核心转换为 V14 形态，`duration` 的旧单位键亦被 schema 清洗删除。两处关键影响：①核心 `#migrateDuration` 按 seconds→turns→rounds 优先序补值，V13 的 rounds+turns 并存形态（如 `{rounds:3, turns:0}`）会被误补为 0 turns 且旧键随即被清洗删除——**该损失无法在运行时恢复**，须在升级前于 V13 侧预处理（AE-14 手动清单）；单独 `{rounds:N}` 或 `{seconds:N}` 形态的补值结果与系统语义一致、无损失；②旧锚点由核心 `_addDataFieldMigration` 正确 rename 到顶层 `start`，剩余时长信息不丢失。因此迁移器在清洗后数据上只做仍可确定性完成的转换：baseChanges（flags 不被清洗）、非被动 showIcon 补设、残留旧键清理；且更新差异手动构造——`diffObject` 默认不生成删除操作（bidirectional 产出的 ForcedDeletion 无法跨 socket 序列化），转换中删除的键以 `-=键` 显式表达（V14 识别并转换为 ForcedDeletion；必填字段如 name 的删除会被 schema 校验拒绝属预期），duration 等整体替换的键由 schema 校验写入时自动清理旧子键；对 schema 外残留键的删除是否落库由 AE-14 验收时核对（残留键本身无害——核心每次加载对其幂等清洗）。**关键实测发现**：非关联 Token 的写回 `token.update({"delta.effects": 数组})` 与 `token.delta.update(...)` 均被核心静默丢弃，唯一有效路径是合成 Actor 的嵌入式文档 `update(diff)`（实测 showIcon/changes/duration/start 全部落 delta 持久化）；孤儿 Token（原型 Actor 已删除）不可经文档 API 触达，跳过并 console.warn 记录、不阻塞版本推进。**机制实测及其定位（testsystem 世界，临时数据已清理）**：Node 断言覆盖转换函数的全部规则（旧格式全字段转换、turns:0 陷阱、seconds 纯时间制、显式 0 保留/显式 1 覆盖、transfer 未写不补、新格式直通、二次转换幂等无变化）；实机首跑 version 0→1、重复刷新与手动重跑均幂等直通无写回、重启后版本达标跳过；注入 version:99 失败阶段实测"前面成功阶段版本保留、失败阶段不推进版本、错误通知、恢复正常后重跑通过"。**定位限定**：注入测试直接改写 `_source`，绕过了核心 server 迁移与 fromSource 清洗，证明的是转换与版本机制本身，不代表真实 V13 存档经完整加载链路后的时长恢复效果——后者归 AE-14 手动验收判定。**阶段 2 决策（2026-09-25 定稿）**：世界宏与用户脚本字符串不做运行时自动转换——对用户手写脚本的自动改写属"整项覆盖"（AE-14 明令禁止），且自动替换存在语法破坏风险（AE-10 转换实践已实证需括号感知+引号转义+语法回归三重防护，不宜放进世界启动流程）。门面归一化继续兜底 `addEffect` 入参类旧写法；读取类旧 API 由用户在升级时按 AE-14 手动清单定点修复。因此阶段表暂维持仅 version 1，未来若需要自动化再以 version 2 追加，已推进到 version 1 的世界会自动补执行新阶段。
- [x] AE-13（S2.2/S2.6/S5.2）核对 V14 `showIcon` 三态：默认 `CONDITIONAL` 只显示有临时时长的 AE；系统希望始终显示图标的通用状态明确设 `ALWAYS`，其他 AE 按现有可见性选择，避免永久状态图标消失。核对 `origin` 的 Document UUID、字符串 `type`/`phase` 与 `@` 值引用；只回归本系统实际用到的变更。`token.*` 变更与 `subtract` 是可选新能力，不作为迁移旧效果的前置。→ **已完成（14.368 实测，testsystem 临时数据已清理）**：`config.mjs` 全部 63 个通用状态条目已显式 `showIcon: 2`（常显）；核心 `toggleStatusEffect`/HUD 路径由核心自动 `showIcon ??= ALWAYS`；origin 与字符串 type/phase 核对已随 AE-01/AE-02 完成。**行为核实与决策（2026-09-25，对照本机 V13/V14 源码 + 源数据盘点）**：V13 Token 只画 `actor.temporaryEffects`，其 `isTemporary = (时长>0) || statuses.size>0`；V14 改看 `showIcon` + `isTemporary`（纯时长），不看 transfer（核心绘制判定：`client/canvas/placeables/token.mjs` 对 `appliedEffects` 过滤 `showIcon===ALWAYS || (CONDITIONAL && isTemporary)`）。据此盘点 data/：757 个被动（transfer:true）全部无时长，两侧均不画，一致；100 个带时长模板两侧均画；646 个无时长且无 statuses 的可施加模板（花酒、食物等）在 **V13 原版即不显示图标**，V14 默认同样不画。**用户决策：所有非被动（transfer:false）施加型 AE 都应显示图标——这是相对 V13 的有意行为变更，不是回归。**施加入口盘点：拖放（`applyDraggedEffect` 内部调 addEffect）与消耗品（medicine 32 处等全部 `game.xjzl.api.effects.addEffect`，无直建）均走门面，但其中经 `toObject()` 的数据携带物化值 1，门面兜底对其不生效，须由数据侧补设（见下方三层分工）。已知边界与实现分工（2026-09-25 二次审阅修正）：V14 schema 对 `showIcon` 为 `required: true, nullable: false, initial: 1`——数据经核心清洗后恒有值，不存在"字段缺失"；消耗品（`item.mjs` 消耗品替换 `toObject()`）与拖放（`applyDraggedEffect` 的 `toObject()`）传入门面的数据必带物化值 1，`??= 2` 对其不生效。**因此实施分三层：①主路径在数据侧——AE-10 为源 JSON 中全部 transfer:false 模板显式写入 `showIcon: 2`（含 646 个无时长与 100 个带时长模板，后者与 CONDITIONAL 行为相同但明确意图）并经 seed 透传，toObject 自然带出 2；②AE-12/AE-14 世界迁移对存量 transfer:false 且 `showIcon === 1` 的改为 2——显式 1 与默认物化 1 在数据上不可区分，鉴于正式世界自 V13 升级（V13 无此字段、1 均为物化值），覆盖显式 1 即达成"所有非被动常显"；显式 0（NEVER，永不显示）保留；显式 1（条件显示）因无法与物化值区分，不在迁移保留范围、一律改写为 2——非被动带时长的效果本就随条件显示语义可见，无时长而需永不显示的效果以显式 0 表达即可；③门面 `#normalizeEffectData` 的 `showIcon ??= 2` 保留为手写脚本入参缺字段时的兜底，不在门面改写显式值 1（避免误伤带时长的条件显示意图）。**另有边界：独立 AE 默认 transfer:true，不在"transfer:false"判据内——带时长者 CONDITIONAL 自动显示，无时长独立 AE 不显示，如需要另行单议。**M2 完成条件实测（2026-09-25，构建 14.368，testsystem 世界，临时数据已清理）：③已落地——`#normalizeEffectData` 末尾 `showIcon ??= 2`，仅兜底缺字段、不改写显式值（含 1）。六条用例：T1 门面缺字段无时长入参→落库 `showIcon=2` 且画布绘制；T2 显式 2 无时长→2 并绘制；T3 显式 2 带时长（3 回合 turnStart）→2 并绘制且 duration 结构正确（时长无关性成立）；T4 显式 1 无时长→保持 1 且不绘制（门面不改写显式值）；T5 拖放路径——对角色卡合成 drop 事件走真实 `_onDropActiveEffect`→`applyDraggedEffect`→`toObject()`→`addEffect`，显式 2 模板施加后目标 AE `showIcon=2`、源文档保留、`origin` 保留；T6 消耗品路径——`item.use({skipDialog})`→`_useConsumable` 的 `toObject()`→`addEffect`，显式 2 模板施加后 `showIcon=2`、consumableType 正确、数量扣减正常。画布 token 视觉确认 5 个图标（T1/T2/T3/T5/T6）恰好绘制、T4 未绘制，与数据判定一致。①源 JSON 补设归 AE-10（经 AE-11 导入验证），②存量迁移归 AE-12/AE-14，均不属于本项完成条件。**`@` 引用只回归实际用到的变更归验收矩阵执行时

### B. 持续时间与架招

- [x] AE-04（S2.4）把添加、满层刷新、非叠层延长、手动时长修改、忍耐减免剧痛、颤手→缴械并卸武器、心火→走火入魔改用 `{value,units,expiry,expired}`。刷新重置顶层 `start`，同单位延长保持旧锚点；比较“更长”时考虑单位和事件，不沿用旧 `rounds*100+turns` 评分。角色/生物卡和状态选取器的剩余时长标签与 `promptEffectDuration` 同步迁移；保留 UI 的 `0=无限`。→ **代码完成并 API 实测（14.368）**：与 AE-02 同批实施（manager 内部时长读写强耦合，无法拆分）。添加/满层刷新重置顶层 `start`（`XJZLActiveEffect.getEffectStart()` 取当前世界时间与战斗位置，替代旧 startTime/startRound/startTurn 手写）并显式 `expired:false`；同单位延长仅累加 `duration.value` 保持旧锚点（实测 rounds 2+3=5 且 start 不变），不同单位交回比较逻辑整体刷新；`compareDurations`/`getDurationScore` 改按秒比较（rounds/turns 依 `CONFIG.time.roundTime` 折算、seconds 原值、日历单位经核心日历换算、不可折算视为无限）；忍耐减剧痛改读 `duration.units==="rounds" && value>0`、颤手/心火转化的缴械/走火入魔直接写 V14 结构；`promptEffectDuration` 读写 V14 结构、默认值取派生 `remaining`（排除无限 Infinity）、`0` 映射为 `{value:null, expiry:null}`（真无限，不写 0 回合）、锚点重置写顶层 `start`；角色/生物卡与选取器标签统一走 `getDurationLabel`（战斗内用核心派生 remaining，战斗外按秒回推回合）。实测：旧格式 `{rounds:2}` 入站后文档为 `{value:2,units:"rounds",expiry:"turnStart"}`；战斗外 5 回合标签显示 `10s`；永久特效标签为 null；全程无弃用警告。待验：忍耐>0 减回合与满层/转化的战斗内实机联调并入 AE-05/AE-06 与验收矩阵场景。**审阅复核（第二轮）修正三项**：①`promptEffectDuration` 默认值曾直接取派生 remaining——战斗外核心把回合时长折算为秒计时（实测 5 回合 remaining=10s），秒数填进"回合"输入框再保存会放大时长，现战斗外与秒制单位按 roundTime 回推回合数（复验弹窗默认显示 5 而非 10），战斗内回合/轮单位直接用 remaining；②同单位延长要求两侧均为有限数值（配合 AE-02 第③项修复限时改无限）；③`compareDurations` 在数值折算等长时增加到期事件排序（turnEnd 晚于 turnStart 到期，实测等值 turnEnd vs turnStart → 1、同 expiry → 0、短 turnEnd vs 长 turnStart → -1），不再让等值但更早到期的效果覆盖现有效果；其余事件对系统未用、视为等价。**审阅复核（第三轮）修正三项**：①到期事件排序曾只覆盖 turnEnd/turnStart，`expiry:null`（秒制归一化的产物）与 turnStart 被视为等价，等时长秒制效果仍可替换较晚到期的回合效果——改为全排序 `null < turnStart < turnEnd`（复验等值 6 秒：turnStart vs null → 1、null vs turnStart → -1、秒 vs 秒 → 0），`getDurationScore` 文档中"到期事件差异不影响比较"的旧注释同步修正为"事件排序见 compareDurations"；②`promptEffectDuration` 曾把战斗内剩余 N 轮的 turns 效果保存为 N rounds 改变到期语义——现保持原单位（弹窗标签同步显示"轮"，保存写 `units:"turns"`，复验输入 3 后 `{value:3,units:"turns",expiry:"turnStart"}`），单位判定回读 `_source` 兜底战斗外派生改写，rounds 效果回归仍为"(回合)"口径。**审阅复核（第四轮）修正**：上轮"战斗外核心把 turns 派生数据改写为秒制"的表述不准确——系统此前只设 `roundTime=2` 未设 `turnTime`（核心缺省 0），战斗外 turns 根本无法折算为秒，核心不走秒制改写而是给出 `secondsRemaining: Infinity`，后果是弹窗把 Infinity 填入输入框（确认时 parseInt→NaN→0，按"0=无限"语义把 1 回合效果误存为无限）、时长标签显示"Infinity 轮"（data/neigong/jiangjunying.json 确有 1 turn 效果）。修正：init 显式 `CONFIG.time.turnTime = 1`（轮次按 1 秒折算，战斗外核心可正常换算）；`getDurationScore` 的 turns 折算同步改用 turnTime（1 秒/轮次）；弹窗默认值与时长标签对非有限剩余量增加"回退总时长"防御，杜绝 Infinity 进入输入框或标签
- [x] AE-05（S2.4/Q5）以 V14 registry + `expiryAction: "delete"` 作为首选清理方式；联调指定目标的 `turnStart`、战斗外每 2 秒一回合、跳回合和 GM 操作，确认不会重复删除或抛错后撤下旧 `updateCombat` 扫描。核心与回合脚本的先后只记录，不单独改调度；若全局 `expiryAction` 引起实际兼容问题，改用 `"update"` 加一个系统清理入口。保留 `cleanExpiredEffects(actor)` 公开兼容语义。→ **已完成（14.368 实测，testsystem 临时数据已清理）**：①init 设置 `CONFIG.ActiveEffect.expiryAction = "delete"`（默认 `"update"` 只标记 `duration.expired` 不删除）；②撤下 updateCombat 六阶段队列末尾的全 combatant `cleanExpiredEffects` 扫描（该扫描不看到期事件、每次流转对所有参战者删除，与核心 registry 重复）；`cleanExpiredEffects` 保留为公开兼容 API，实测空操作不报错、回拨 `start.time` 制造过期后手动调用可物理删除。**核心契约核实（本机源码 + 实测）**：registry 注册/注销由核心托管（Actor 建档/特效 create-update 时按 `isExpiryTrackable = persisted && !inCompendium && isEmbedded && active && start && isTemporary` 重估，被抑制/永久/无 start 的特效不入册）；`refresh` 事件点覆盖 combatStart/combatEnd/roundStart/roundEnd/turnStart/turnEnd/updateWorldTime，删除动作仅 `activeGM` 执行；**跳回合不漏删**——核心 `_manageTurnEvents` 对被跳过的中间回合逐个补发 turnEnd/turnStart（`skipped:true`）；**战斗外计时**——rounds 按 roundTime=2 折算为秒，`updateWorldTime` 对不在战斗的 Actor 满足回合类到期事件。**实测记录**：①战斗内 3 回合 turnStart 特效施加于甲的回合（start 锚定 combat/combatant/round），乙的回合开始时剩余 2/1/0 均不移除（他人回合不提前删），甲的回合开始剩余 0 时物理删除恰好一次，继续推进 2 次流转无复活、无重复删除报错；②破防（1 回合）甲回合施加于乙、`nextRound` 跳回合后，在施加槽位（甲）的下一回合开始精确删除；③战斗外 3 回合特效派生 6 秒，`game.time.advance(5)` 后存活（余 1 秒）、推过到期点后由 updateWorldTime 刷新删除（注：删除经 modifyBatch 异步落地，紧随 advance 后立即查询可能仍见旧值，需等一拍）；④全程控制台除测试自造输入错误外零错误。**顺序记录（不改调度）**：`updateCombat` 钩子在 `Combat#_onUpdate` 的 `super._onUpdate` 中先派发，核心 `_manageTurnEvents`（turnEnd→roundEnd→roundStart→turnStart，registry.refresh 内嵌各阶段）随后以浮动 Promise 执行，与我方 `EncounterManager.queueCombatUpdate` 六阶段队列并发交错，实测无冲突无重复清理。**语义差异记录**：核心 `turnStart` 到期锚定施加时点的当前行动者槽位（`start.combatant`），而非目标 Actor 的槽位——旧扫描等价于"轮次边界即删"，核心语义为"施加槽位后第 N 次同名槽位回合开始删"；多角色战斗中删除时点可能晚于旧轮次边界（目标在其间的回合仍带状态），业务表现为"持续 N 回合"不少于一整轮，按 Q5 约定不作为产品约束，如实战反馈不符再单开工作项评估逐条改用回退方案
- [x] AE-06（S2.4）保留并回归 `tiedToStance` 业务清理；验证同武学豁免依赖的 Item `origin`、架招切换/解除/被破除。拖放绑定 AE 时先确认来源与目标 Actor 当前使用同一架招，确认后复制，保留 flag、原 `origin`、`duration` 与 `start`，交由目标架招清理；不相同或无法确认就结束此次施加，不创建目标 AE，也不新增时长或解绑特例。→ **已完成（14.368 实测，testsystem 临时数据已清理）**：①业务清理零改动回归通过——`stopStance`（主动解除/被破除/濒死共用路径）实测清空 `martial` 三字段并删除全部绑定特效；`clearStanceTiedEffects(excludeOrigin)` 同武学豁免实测只清 origin 不同的绑定特效、保留 origin=当前武学 uuid 的特效（豁免依赖脚本写入 `origin=thisItem.uuid`，见 AE-10 备查）；②新增 `ActiveEffectManager.getActiveStanceSignature`/`canApplyStanceTiedEffect`：合集包来源相同时按招式名比较，否则按武学名和招式名比较，任一方未开启架招、武学缺失或招式无法定位一律拒绝；实测同名武学同名架招→允许、同武学另一架招→拒绝、未开启→拒绝；③新增 `applyDraggedEffect` 统一拖放施加入口（角色卡 `_onDropActiveEffect` 与画布 `dropCanvasData` 共用）：同架招时复制成功且 flag/origin/duration/start 与源逐项一致、源文档保留、目标仅一份副本，异架招/无来源时在任何叠层、脚本、飘字、聊天副作用之前中止。**备查**：V14 `origin` 为 DocumentUUIDField 自校验，写入非法 ID 会被核心创建校验拒绝（实测 "Item.fakeOldWuxue" 创建报 "must contain a valid document ID"），数据迁移时不得保留非法 origin；物品内嵌 transfer:true 被动拖放拒绝、普通 AE 拖放照常施加的用例随 AE-08 覆盖

### C. 编辑器、拖放和状态选取器

- [x] AE-07（S2.5）将 `XJZLActiveEffectConfig` 迁至 V14 ApplicationV2 的 `PARTS`/`TABS`/`actions`，保留核心 details/duration/changes 页签与 `showIcon`、`transfer`、`statuses`、新变更字段；加入“高级规则配置”页，完整保留 slug、可叠层、最大层数、`tiedToStance`、脚本标签/时机/启停/代码、增删和变更 key 的 datalist。兼容对象形态的旧 scripts；验证跨页签未保存字段、物品/Actor 内嵌及合集包独立 AE 的保存与重开。→ **已完成（14.368 实测）**：移除 V1 的“继承核心 + render 钩子注入页签 + defaultOptions/getData”兼容写法，改为标准 ApplicationV2 子类：完整重声明核心 PARTS（header/tabs/details/duration/changes/footer，`this.constructor.PARTS` 子类整体遮蔽父类、不做链式合并）并追加 xjzl part；TABS 追加“高级规则配置”（label 直接给 `XJZL.Effect.ConfigTitle`，不依赖 labelPrefix 拼核心键）；增删脚本为 DEFAULT_OPTIONS.actions 动作（`_onAddScript`/`_onDeleteScript`，先读当前表单未保存状态再整体 `submit({updateData})`，数组在 mergeObject 中整体覆盖故删除中间项不残留）；`_processFormData` 兜底数字键对象 scripts → 数组。实测（Actor 内嵌/Item 内嵌/合集包独立 AE 三种来源）：①4 页签渲染与切换正常（真实点击 nav），核心 duration 页 V14 字段（value/expiry 事件）与 details 页 transfer/statuses 完好；②添加脚本→DB 落一条、未勾选 active 经 FormDataExtended 正确存为 false（V14 checkbox 语义，V13 旧顾虑不存在）、再添加→2 条、删除 index 1→剩 1 条且首条 label/active/script 内容完好；③跨页签：在 header 改 name 后不切页直接点添加脚本，name 与 scripts 随整表单提交同时持久化；④关闭重开全部字段还原；⑤changes 页“添加变更”后 key 输入自动挂 datalist（43 个候选项，`input[name$=".key"]` 由 _onRender 挂载）；⑥全程控制台零报错
- [x] AE-08（S2.10/S2.11）在武学、内功、装备、特性、身世、通用物品六类现有 AE 模板上，为可施加的 AE 行加入 V14 识别的 `.draggable`、`data-effect-id`；角色/生物卡的非物品被动 AE 行也使用核心拖拽。Item 内嵌 `transfer:true` 被动 AE 在物品页与 Actor 页均不可拖。角色卡接收 AE 走 `_onDropActiveEffect` → 门面，画布 Token 在 `dropCanvasData` 阶段拦截核心直建并走门面，均不得重复创建；Item 卡接收可拖 AE 仍复制模板。对 `tiedToStance` 施加先执行 AE-06 的双方架招判定，失败时中止而不触发叠层、脚本、飘字或聊天。检查角色卡现有 Item 拖拽监听不吞 AE 事件。用临时独立 AE 合集包验证原生打开、编辑、拖放，不建立正式 AE 包；覆盖绑定 AE 的同架招成功、异架招/无架招/无来源失败，成功时 flag/origin/start/duration 保留，以及独立 AE 不按 `transfer` 误挡、权限、合成 Actor 与来源名回退。→ **代码完成并 API 实测（14.368，临时数据已清理）**：①六类模板 effect 卡加 `{{#unless effect.transfer}} draggable{{/unless}}` + `data-effect-id` + 可拖行 DragHint 提示，wuxue/neigong 的 effects 上下文补 `transfer` 字段；物品 sheet 均继承核心 ItemSheetV2 的 DragDrop（`.draggable` 选择器 + `_onDragStart` 读 dataset.effectId → toDragData），实测被动行无 draggable、可施加行 attr=true 且合成 dragstart 产出 `{type:"ActiveEffect", uuid}`；②角色/生物卡特效 chip 加 `draggable` 类（data-effect-id 已有），**发现并修复**：角色卡自定义 `_onDragStart` 遮蔽核心实现、chip 无 `data-item-id` 时直接 return 导致拖拽数据为空——改为无 itemId 时 `super._onDragStart(event)`，实测 chip 拖拽数据恢复 `ActiveEffect` UUID，生物卡走核心路径原生可用；③角色/生物卡覆写 `_onDropActiveEffect` → `ActiveEffectManager.applyDraggedEffect`（AE-06 门面：被动拦截、架招判定、叠层/飘字/聊天语义），实测同架招施加成功仅一份、异架招画布与 sheet 两路均中止；④`xjzl-system.mjs` 注册 `dropCanvasData` 钩子：ActiveEffect 类型落点按核心同款 quadtree 碰撞取 Token 后走门面并返回 false，实测钩子返回 false、核心直建不发生、重复拖不重复创建；⑤Item 卡接收沿用核心 `_onDropActiveEffect` 模板复制，实测复制后 transfer 原值保留；⑥临时独立 AE 合集包（`CompendiumCollection.createCompendium`，type ActiveEffect）：`fromDropData` 解析、`isEmbedded=false`、AppV2 编辑器打开编辑正常（AE-07）；独立绑定 AE 无来源拒绝、独立普通 AE（transfer 默认 true）不误挡且 origin 缺失时补源 UUID；⑦核心来源名回退：独立 AE origin 指向合集包文档时 `sourceName` 按核心显示 Unknown，未改写合法 origin。**待验**：玩家端拖放经 socket 委托的完整链路待 S5.2/S5.4 多端回归；合成 Actor（非链接 token）落点走 `target.actor`（S0.4 已验证合成 Actor 特效落点行为，本次未重复覆盖）。权限与来源名回退的玩家视角回归归验收矩阵执行。**审阅复核（第一轮）修正两项**：①`prepareEffects` 基于 `appliedEffects`（含装备衍生特效）填充 `temporaryEffects`，有时长的 transfer:true 物品 AE 会渲染成 chip 且模板无条件加 `draggable`，与"物品被动不可拖出"不一致（核心拖拽按 `actor.effects` 查 id 实际也拖不出数据）——分类上下文新增 `isActorEffect`（仅 AE 内嵌于 Actor 时为真），角色/生物卡 chip 模板改按该标志门控 `draggable`，实测装备武器限时被动照常显示但不可拖、角色特效照常可拖；②`dropCanvasData` 钩子的异步施加未捕获异常且立即返回 false，失败时核心落点已被拦截而用户无感知——async 体包 try/catch，`console.error("XJZL | ...")` 记录上下文并 `ui.notifications.error` 反馈（新增 `XJZL.Effect.DropFailed` 键），实测非法 UUID 拖入无未捕获 rejection 且通知触发。**补记（2026-09-25）**：Actor 内嵌 AE 拖放到其他 Actor 时，目标施加成功后删除来源；物品模板、合集包 AE 和 Item 卡接收仍为复制。GM 端转移实机复验通过。玩家无来源编辑权限时，现有 GM 删除委托未校验该权限；来源删除失败时目标已施加，可能留下两份。两条路径待修复并复验（S5.2）。
- [x] AE-09（S2.8）状态选取器继续以 `CONFIG.statusEffects` 展示标准状态，并保留场上物品 AE；回归分类计数、搜索、最近、收藏、已生效态、左右键叠层/时长、撤除、受控/瞄准目标和多目标。同步回归 TokenHUD 点击、overlay 与 `actor.statuses` 判断；更新旧 `duration.rounds` 读取。**不新增必须维护的合集包 AE 分类**。→ **完成（Foundry 14.368；testsystem 临时数据已清理）**。
  - 代码核对：状态盘和角色卡的时长标签走 `ActiveEffectManager.getDurationLabel`，手动设置时长写入 V14 duration 与 start；模块内旧 `duration.rounds/turns` 只用于 AE 入参归一化。
  - 状态盘实测：10 个分类的计数与可见项一致；中文名、ID 和场上特效搜索，最近与收藏，当前状态和叠层显示，左右键加减层、修改时长、撤除，以及框选、瞄准和多目标施加均通过。时长设为 3 回合后落库为 6 秒并保留 `turnStart` 与 `start.time` 锚点；设为 0 时写入 `value:null`、`expiry:null`。
  - TokenHUD 与状态实测：63 个状态图标可用；点击施加、刷新和移除不重复创建。死亡流程的 AE 带 `flags.core.overlay:true`，画布覆盖图标以 `token.effects.overlay` 验证；`actor.statuses` 和 `effect.statuses.has()` 判断正确。
  - 窗口生命周期实测：状态盘的工具栏、角色卡和战局入口统一复用窗口；伤害工具使用相同的 V14 实例登记、首次渲染暂存与关闭中等待机制。两工具首次连点、关闭动画中重开及并发重开均保持单例，关闭后 Hooks 清理；模拟旧窗口关闭失败时停止新建、记录错误并通知用户。
  - 非 AE 待办：审计日志单例和容器需求结果的窗口刷新仍使用 `ui.windows`，归总计划 S5.3。

### D. 源数据、合集包与世界迁移（与 M4 共用工作项，不因 M2 完成而提前勾选）

- [x] AE-10（S4.1/S4.2/S4.6，及 S4.8 的 AE 相关脚本审计）按 AE 对象结构遍历 `data/**/*.json`：`changes→system.changes`、数字 `mode→type`、`icon→img`、duration、`origin`、`showIcon` 的实际需要；按脚本字段和变量来源迁移动态构造、`effect.changes`、状态查询和时长读取。既有约 500 处 `CONFIG.statusEffects.find`、98 处 AE `.changes` 只是检索基线，需按谓词/变量来源分类并补查其他别名。检查 `module/config.mjs` 的通用状态模板；不对整个 JSON 文件盲目替换。**执行 AE-13 决策：为源 JSON 中全部 transfer:false 模板显式写入 `showIcon: 2`**（V14 schema 默认物化 1，不写入则 toObject 路径无法达成常显）。**各 seed 的 effects 构造需透传 `showIcon` 字段（现缺——源 JSON 写入值会在重建合集时丢失，见 seed-consumables.mjs 等）**。同步更新 `SCRIPT_ENGINE.md`：示例全部改为 V14 新写法，并明确旧写法入参仍被门面兼容、但推荐新写法；按录入格式变化更新 `SEEDING_GUIDELINES.md`。→ **范围决策（2026-09-25，用户确认）**：系统自带 data/ 一律用 V14 新写法、不依赖归一化——脚本字符串内联的旧 `duration:{rounds:...}` 等入参也随本项改为新格式；门面归一化层保留，定位降级为"世界宏/玩家脚本等外部调用的兜底"（SCRIPT_ENGINE.md 承担对新写法的指导）。**M2 期间实测盘点基线（2026-09-24，供实施时对照；2026-09-25 两轮复测修正口径）**：data/ 中 `game.xjzl.api.effects.addEffect(` 原始静态检索 **2021 处、分布于 86 个 JSON 文件**（旧基线曾记 1815/69，为不同统计口径与计数脚本缺陷，以本次为准），其中内联 `duration:{rounds:...}` 以静态检索精确为准为 **156 处、分布于 41 个 JSON 文件**（旧基线记 660 处为宽口径、已作废）——原判定由归一化承接、无需迁移，现按上述决策一并改为新写法；物品模板 effects 内旧 duration 精确 113 处（丹药/消耗品类为主）；需迁移的另有约 101 处旧文档路径读取（`effect.changes`、`thisEffect.duration.rounds` 等，含 `eff.changes` 别名）与约 478 处 `CONFIG.statusEffects` 直查（改 `getStatus`）；未发现绕过门面直建 AE 的调用（无 `createEmbeddedDocuments("ActiveEffect")`/`new ActiveEffect`/`ActiveEffect.create`）**实施完成（2026-09-25，Node 离线批量转换 + 人工复核，构建 14.368 实机经 AE-11 导入验证）**：①模板 effects 2378 条全部转换（转换规则复用 `module/utils/effect-conversion.mjs`——与 AE-12 世界迁移同一套事实源：icon→img、顶层 changes→system.changes、mode→type、duration 旧键处理+锚点核对、baseChanges），其中 1365 个 transfer:false 模板显式补 `showIcon: 2`（AE-13 决策①）；②脚本字符串转换：`CONFIG.statusEffects.find(e=>e.id===X)` → `game.xjzl.api.effects.getStatus(X)` 516 处（字面量与变量形态）、内联 duration 字面量/表达式约 750 处改 `{value,units,expiry}`（rounds/turns 默认 turnStart、seconds 为 expiry:null）、内联 `mode:2/5` 71 处改字符串 `type`、`X.changes` 读写 169 处改 `X.system.changes`（18 个变量名逐一上下文核实）、`duration.rounds` 读写 12 处改 `duration.value`、AE 数据对象 `icon:`/`icon: item.img|thisItem.img` 约 61 处改 `img:`（DialogV2 的 fas fa-* UI 图标保留）、内联 AE changes 数组 28 处括号感知包裹为 `system:{changes:[...]}`（customModifiers 等非 AE changes 保留）；③两处系统性风险经专项扫描修复：JSON 字符串值内的替换按解码态文本进行（正则覆盖面比转义态基线更全），嵌套字符串字面量内的注入引号按外层定界符转义（4613 个脚本 AsyncFunction 语法回归对比 HEAD，2 处残留为原有占位符/说明文本，非转换引入）；④`medicine.json` customModifiers 的 changes 无 type/mode 键，判定为系统数据未误转；⑤AE 相关已移除 API 审计（S4.8 的 AE 部分）：`blindData.label`→`name`、`${thisEffect.icon}`→`img` 两处修复，其余 `.label`/`.mode`/`CONFIG.label` 命中均为 UI 选项、表单元素或系统自定义 flag，非 AE 文档字段；⑥seed 透传：seed-armor/consumables/neigong/qizhen/weapons/wuxue 六个白名单映射构造补 `showIcon: e.showIcon`（traits/misc 全对象透传自带、origins 动态被动构造无此需求），过时注释同步更新；⑦文档：SCRIPT_ENGINE.md 状态 API 章节补 V14 入参结构示例与"门面兼容旧写法仅供外部兜底、读取路径必须新写法"约定，SEEDING_GUIDELINES.md 录入规则与验收清单补 V14 结构与 showIcon 要求。**全库终态校验**：duration 块 962 处中 960 处为 V14 三键结构、2 处为有意的空 duration（永久语义，注释明确）；旧检索模式（inlineRounds/statusFind/modeNum/durationRoundsRead）清零；全部改动 JSON 通过 JSON.parse。**AE-11 导入时补充发现并修复**：neigong/consumables/wuxue 三类源 JSON 中 88 个 effects 条目未写 `transfer`（seeder 默认补 false 的隐式施加型），`transfer === false` 谓词未命中——已按 seeder 默认语义显式补 `transfer: false` + `showIcon: 2`。
- [x] AE-11（S4.4）源 JSON、seeding 和脚本审计完成后重建现有 Item 等合集包并检查导入结果；不为每个内容专属 AE 建独立合集包。若未来要建立固定 AE 模板包，先明确唯一素材事实源和 seed 规则。**导入验证须含 AE-10 的字段透传：合集包模板携带 `showIcon: 2`，经施加后仍为 2（常显生效）。**→ **完成（2026-09-25，构建 14.368 实机，testsystem 世界）**：①按 `game.xjzl.seed` 全部 11 类合集包重建（origins/consumables/artbooks/misc/armor/weapons/qizhen/neigong/wuxue/macros/traits，等价 `seed.all()` 序列）；②导入校验：2387 个合集包内嵌 AE 全部为 V14 结构（`img`、`system.changes` 数组且 `type` 为字符串、无 `icon`/`label`/顶层 `changes`/旧 duration 键）；**showIcon 透传：1438 个 transfer:false 模板全部 `showIcon: 2`，零不合规**；③首跑发现并修复一个 AE-10 遗漏口径：neigong/consumables/wuxue 三类源 JSON 中 88 个 effects 条目未写 `transfer`（seeder 默认补 false 的隐式施加型），AE-10 的 `transfer === false` 谓词未命中——已按 seeder 默认语义显式补 `transfer: false` + `showIcon: 2`（armor/weapons/qizhen/traits 的 seeder 默认 true/schema 默认 true 属被动不补；分布统计 803 显式 true/210 undefined 已逐类核对）；④端到端常显验证：从合集包取无时长施加型模板（"黄金比例烧麦"，showIcon:2）走 `toObject()`→`addEffect` 施加，落库 AE `showIcon: 2` 且核心绘制过滤命中、画布 token 图标视觉确认（AE-13 决策①→seeder 透传→施加路径全链路闭环）；⑤迁移后脚本的可执行性已由 AE-10 的 4613 个脚本 AsyncFunction 语法全检覆盖，实际脚本运行归 S5.4 完整战斗回归；⑥临时数据已全部清理。
- [ ] AE-14（S4.7）在备份的 V13 世界副本实施并验收 AE-12 的迁移：Actor/Item/非关联 Token AE、世界物品、导入宏和用户脚本；核对 `baseChanges`、duration、变更与图片字段、既有数值和可恢复的剩余时长；按 AE-13 决策补设 `showIcon`——覆盖存量已生效效果与**世界物品及 Actor 内嵌物品中的 transfer:false 模板**（值为 1 → 改为 2；显式 0 保留），模板不补设则其 toObject() 再施加时门面兜底不生效。验证首次执行、重启、重复运行及中断重试，含分阶段版本标记下后扩展转换项的补齐执行；用户改过的脚本只做可确认的定点转换，无法识别的记录 UUID/字段位置和处理方案，不整项覆盖。
→ **移交用户手动验收（2026-09-25 用户确认：涉及挂载/重启 Foundry 实例的环境操作由用户手动执行，会话不做）。**原定验收方式为复制 v13data 中的 V13 世界副本挂载到 V14 实例运行迁移；因副本世界的挂载需要 setup 页面（admin 密码会话不可知）或重启 Foundry 服务，会话曾在未事先征得同意的情况下重启过服务并因此触发 license 引导，已恢复全部配置原状（admin.txt、options.json 均已还原；临时副本世界 xjzl-v13test 已删除，v13data 源世界未受影响），本次事故不影响系统代码与数据。**已完成的机制验证（正式验收时不需重复）**：AE-12 阶段 1 迁移机制已在 M2 用注入数据实测——首跑 version 0→1、重启/重复运行幂等直通、阶段级失败不推进版本号且恢复正常后自动补执行、Node 断言覆盖转换规则、非关联 Token 经合成 Actor 嵌入文档写回路径落地（showIcon/changes/duration/start 均持久化）、delta.items 内嵌 AE 转换实测；duration 整体替换的写回行为已实测。**定位限定**：注入测试绕过了核心 server 迁移与 fromSource 清洗，真实 V13 存档经完整加载链路后的时长恢复（rounds+turns 并存陷阱的预处理必要性）由本验收判定。**用户手动验收步骤（正式世界从 V13 升级时执行，或先用 v13data 副本世界演练）**：
1. **备份**：完整复制 V13 世界目录留存后再开始；
2. **升级前预处理（必须，规避数据损失）**：在 V13 实例中于目标世界以 GM 运行预处理宏 [`docs/migration/v13-ae-preprocess.js`](migration/v13-ae-preprocess.js)——宏遍历全部 Actor/Item/非链接 Token 合成数据的 AE，把 rounds+turns/seconds 并存的 duration 合并为单一主单位（rounds 优先，规避 V14 清洗的 turns 优先陷阱：`{rounds:3, turns:0}` 会被误补为 0 turns 且不可逆），输出控制台报告并自动下载 JSON 核对清单（记录升级前的原始时长形态）；升级后将该清单与 V14 导出的 AE 结构对照，逐条核实时长正确性（这是 rounds+turns 并存数据在迁移后唯一可靠的核对路径，已加载的 V14 文档上旧单位键已不可见）；宏幂等可重复运行，**报告中 `unverified` 与 `errors` 都必须为 0 才允许升级**——任一非零都意味着仍有并存形态的旧时长未合并，V14 加载时将不可逆丢失；处理失败条目（按报告 `path` 定位修复数据或手动合并）后重跑宏直至全部归零；
3. **首跑**：用 V14 实例打开 V13 世界，以 GM 登录。ready 阶段迁移自动执行（仅 activeGM 端），控制台应出现 `XJZL | AE 世界迁移阶段 1 开始/完成` 日志与"迁移完成"通知；
4. **结构核对与持久化证明（两次）**：首跑后与步骤 6 重启后各抽查一次若干 Actor/Item/非关联 Token 的特效——`_source` 应为 V14 结构（`img`、`system.changes` 数组且 `type` 为字符串、`duration` 三键）；`flags.xjzl-system.baseChanges` 已转字符串 `type`；已生效特效顶层 `start` 有值（角色卡剩余时长标签正常，不因迁移清零）。**持久化证明的依据**：`baseChanges` 的 `type` 转换与非被动的 `showIcon: 2` 补设是系统迁移独有的写回——核心加载清洗不做这两件事，因此重启后这两类转换仍然存在，即是迁移写回已落库的直接证据（`_source` 表面干净不足以自证，旧字段可能只是被每次加载重新清洗）。辅助手段：首跑后与重启后各用一次世界导出/AE 导出留存记录，前后对照应一致且含上述独有转换；
5. **showIcon 补设**：transfer:false 的存量特效与模板 `showIcon` 应为 2（画布图标常显，与时长无关）；显式 0 的效果保持不显示；
6. **版本与幂等**：`game.settings.get("xjzl-system", "aeMigrationVersion")` 应为 1；重启世界后无迁移日志、数据无二次变化；手动把该 setting 拨回 0 再重启，应只做幂等直通、无重复创建或叠层；
7. **数值抽样**：挑选若干已知数值的被动效果，核对迁移前后加成数值一致（mode 语义保留，尤其 mode:5→override）；
8. **失败情形**（可选演练）：迁移报错时版本号不推进并出现错误通知，修复数据源后重启自动重试；
9. **脚本字符串范围（AE-12 阶段 2 的产品决策，2026-09-25）**：世界宏与用户脚本不做运行时自动转换——对用户手写脚本的自动改写即 AE-14 所禁止的"整项覆盖"，且脚本形态多样、自动替换存在语法破坏风险（AE-10 转换实践中已实证）。实际影响与处理：`addEffect` 入参类旧写法（duration:{rounds}、icon、顶层 changes/mode）由门面归一化长期兜底，**不影响运行**；`CONFIG.statusEffects.find`、`effect.changes`、`duration.rounds` 等**读取类旧 API 不被兜底**，升级后会运行时报错——升级前应按 AE-10 的转换规则人工盘点修正（data/ 的同类转换可作参照），或升级后按报错逐个定点修复，无法识别的记录 UUID/字段位置和处理方案。

→ **首跑演练通过（2026-09-25，V13 世界数据在 V14 首次加载）**：阶段 1 迁移日志显示更新 50 条特效，未见报错，`aeMigrationVersion` 为 1。另抽查了 V14 新建状态特效的 `img`、`showIcon: 2`、`system.changes`、`baseChanges`、`duration` 和 `start`；该抽查不证明升级前特效已正确迁移。本次未在 V13 运行预处理宏，不能据此验收旧特效的剩余时长。正式迁移前须备份，并在 V13 运行预处理宏、确认报告中的 `unverified` 与 `errors` 均为 0；随后按步骤 4～9 核对旧特效结构、数值、时长、重启持久化、幂等性和用户脚本。失败重试按步骤 8 视需要演练。AE-14 保持未完成状态。

### 实施顺序与出口

1. M2 先完成 AE-01/AE-02/AE-03 的数据与公开入口，AE-13 同步核对 V14 契约；以测试数据让 AE 可正确创建、修改和生效。
2. AE-04 改完时长读写后实施 AE-05 的核心过期清理，AE-06 保留架招业务清理；检查到期目标、2 秒/回合及不重复删除，脚本与 registry 的先后不是通过条件。
3. 完成 AE-07/AE-08/AE-09 的编辑、拖放与状态选取器回归；M2 出口不依赖正式 AE 合集包或旧世界迁移。
4. M4 按 AE-10 → AE-11 迁移源数据并重建现有合集包，再以 AE-14 验收旧世界副本；AE-12 迁移框架在 M2 建立。非 AE 的 M4 工作仍按总计划执行。（2026-09-25 曾提议把 AE-12 排到 AE-10/11 之后，经审阅指出与总纲 M2 出口/M4 依赖及 S4.4 前置冲突，已撤销，维持本条原次序。）

## 验收矩阵与完成标准

| 场景 | 必须看到的结果 |
|---|---|
| 三回合状态，在 A 回合给 A 施加 | 到期只在 A 的指定回合开始处理一次，不重复触发、漏删或报错；其他角色回合不提前移除。记录实际起点与回合脚本顺序即可。 |
| A 回合前/后由他人施加；战斗外施加再进战 | 记录起点和实际剩余时长；战斗外世界时间每推进 2 秒抵 1 回合，到期清理与进战衔接正确。 |
| 叠层、满层刷新、非叠层重施加、延长与手动改时长 | `stacks/maxStacks/baseChanges` 与数值乘算正确；来源、覆盖、延长和刷新规则保持；角色/选取器显示剩余时长正确，`0=无限` 不会立刻过期。 |
| 忍耐、颤手、心火 | 剧痛减回合和完全豁免提示、颤手满层转缴械并卸武器、心火满层转走火入魔的时长/飘字/聊天卡片与旧规则一致。 |
| AE 挂载脚本、飘字与聊天设置 | `flags.scripts` 的 `passive`、回合等既有触发器照常收集并执行；创建、叠层、减层、删除的飘字不重播；`showEffectChatCards` 开/关分别符合设置且不重复发卡。 |
| 被动装备、破衣抑制、性格自动 AE、消耗品 AE、破防与死亡/濒死状态 | 既有创建、抑制、替换、删除、状态图标和脚本结果保持；核心直接创建的路径也使用 V14 数据格式。 |
| 物品、角色/生物卡和独立 AE 拖放 | Item 内嵌被动 `transfer:true` 在物品页与 Actor 页均不可拖出，也不能通过目标端施加；可施加 Item AE、普通 Actor AE、独立 AE 能落到角色/Token，可施加 AE 在物品间能复制。绑定架招的 AE 仅在来源/目标 Actor 当前架招相同时能施加；否则目标没有副本或任何施加副作用。成功复制时源文档保留、目标只有一份副本，flags/脚本/changes/原 `origin`/原 `duration` 与 `start` 按来源保留。 |
| 状态选取器、特效卡片和编辑器 | 分类、搜索、最近、收藏、场上特效、多目标、左右键加减层/改时长、删除、TokenHUD/overlay、页签和脚本编辑均可用；`actor.statuses` 与变更 key 自动补全仍正确。 |
| 玩家、GM、非关联 Token | 权限委托成功且操作落在正确的 Actor；不重复施加/飘字/聊天卡片，也不能绕过目标权限。 |
| 架招结束或切换 | 来源和目标都启用同一架招时，拖放副本由目标角色现有 `tiedToStance` 清理规则处理；异架招、缺架招或来源无法确认时不创建副本。既有同武学豁免不误删，自身时长到期也不漏删。 |
| V13 世界副本迁移、重启、重试 | 旧 AE 数值与可恢复的剩余时长不意外变化；不可恢复的锚点有明确记录；第二次执行不重复创建或叠层，失败保留定位和重试能力。 |

每个改动的 `.mjs` 执行 `node --check`，每个改动的源 JSON 执行 `JSON.parse`；实机记录使用的 Foundry 构建号、操作步骤、结果和未覆盖项。**M2 出口**是代码链路、编辑器、拖放和状态选取器在 V14 测试数据上通过；**M4 出口**才包括源 JSON、合集包及旧世界副本迁移。未通过实机验证的项保持 `[ ]`，不以源码推断代替验收。
