# V14 Region / 光环专项计划

本文件是 [`V14_PLAN.md`](V14_PLAN.md) 中 M3（S3.1–S3.7）与 S4.3 光环宏改写的专项计划文件，对应总纲 §3 与 Q4 决策。AE 侧实施仍以 [`V14_AE_PLAN.md`](V14_AE_PLAN.md) 为准，本文件不复述 AE 契约。

**当前状态（2026-09-26）**：盘点、分类、实现框架决策与专项开发计划（第 7 节）均已定稿，并经外部审阅与数据作者多轮修订（决策 14～39）；AURA-01 机制 spike 与 AURA-02 形状生成器已完成（结论见第 7.2 节对应条目）；**AURA-03～05 框架三件套代码已完成并经三轮静态审阅修复（2026-09-26，`node --check` 与形状断言通过，接受的边界已记录，实机验证留下次会话，状态保持未勾并注明"代码完成待实机"）**。第 1～5 节是事实基础与 94 条盘点清单，第 6 节是决策记录，第 7 节是唯一施工与验收依据。

## 1. 盘点口径

- 盘点范围：`data/**/*.json` 源数据。数据作者在需要光环自动化效果的条目上打了标记键 `"x-xjzl-aura": true`，盘点以该标记为准（解析 JSON 后按对象键定位，非文本检索；94 处全部为结构性标记，脚本字符串中无该标记）。
- 标记位置约定：neigong（19）、armor（1）、weapons（6）打在条目 `system` 上；wuxue（68）打在 `system.moves[]` 的招式对象上。共 44 个文件、94 处。
- 后续规则书新增光环内容时，沿用同一标记约定，并回填本文件对应分类表。

## 2. 现状与既有基础

- **payload AE 已按物品级 effects 承载**：光环的效果本体大多已录入为物品级 effects 条目（`transfer: false`，按等级/境界分档命名，如"无涯 (1级)/(2级)/(3级)"、"剑曲绝鸣 (敌方减益·领悟/掌握/精通)"），并已在 AE-10/AE-11 完成 V14 结构迁移与 `showIcon: 2` 常显。缺 payload 的条目多为依赖系统状态（目盲、禁足等）或纯规则效果。
- **当前施加方式全部无区域支持**：手动拖拽 payload AE 给范围内单位、combatStart 等脚本一次性分发（马头琴、书香江南、惜余琴自身、青海）、或事件脚本提示手动结算。`automationNote` 逐条记录了现状。
- **旧可视化工具已停用**：`data/macros/utility.json` 的"⭕ [快捷] 创建跟随光环"宏基于 MeasuredTemplate（S1.14 已停用），其改写归 S4.3，并入本专项。
- **脚本式光环先例**：水中战斗（青海）AE 以 `flags.xjzl-system.radius` 存半径、`turnEnd` 脚本做维持消耗与广播——证明"AE 自带脚本 + 区域参数"路线可行，但无法覆盖进出结算与可视化。
- **S0.4 机制结论（光环实现的地基）**：Region 行为 `applyActiveEffect` 可对进出 Token 施加/移除特效；**区域进出/回合事件的派发口径（AURA-01 复测修正，见第 6 节决策 39）**：官方移动与 `document.update` 裸传送均派发进出事件，`updateSource` 纯内存写则无钩子、无事件、包含性跟踪也不更新；**非链接 Token 的 `token.actor` 是合成 Actor**，光环施加/清理必须处理该分支；`RegionDocument.createTokenEmanation` 可生成跟随区域；`attachment.token` 提供原生跟随（AURA-01 实测：须传 token id，grid 形状随源整格平移，源删除自动删区并补发 exit）。

## 3. 实现框架（2026-09-26 与数据作者讨论定稿）

讨论结论：**只实现一个自定义 Region 行为类型**（暂名 `xjzlAura`），原甲/乙两类是它上面两个互相独立的结算开关；原丙/丁/戊/己不再是独立实现类别，分别折叠为开关选项、监听 AE 形态、脚本分发和仅标记形态。第 4 节清单保留原分类名用于对账，实际实现形态按本节归属。

条目分布：进入/回合结算开关承载 66 条（原甲 45 + 原乙 21 + 原丙 2 条主体；第三轮审阅将五雷、马头琴按规则原文改归编排与分发）；监听 AE 7 条；脚本编排 10 条；脚本分发 2 条；仅标记 6 条；保持手动 2 条；待澄清 1 条。

### 3.1 行为类型与结算开关

| 结算开关 | 事件 | 动作 | 选项 |
|---|---|---|---|
| 进入结算 | `tokenEnter` / `tokenExit`（对称：进入生效、离开撤销） | 挂 payload AE / 直接伤害治疗 / 两者选配 | 每回合限一次（节流，吸收原丙类首进型） |
| 回合结算 | `tokenTurnStart` / `tokenTurnEnd` / `tokenRoundStart` / `tokenRoundEnd`（核心 Combat 文档在指定 GM 端派发，且派发顺序在 ActiveEffect registry 刷新之后） | 伤害 / 治疗 / 挂 AE / 维持消耗（资源不足即散，青海脚本先例） | 可与进入结算同开（如水牢：进入伤害 + 回合开始流失） |
| 仅标记 | 无结算 | 只渲染范围 | 原己类用；配创建时说明卡 |

行为 schema 其余字段：半径（米，按 1-2-2-2 折算格子集合）、阵营过滤（友方/敌方/全体）、是否含自身、持续回合数（0=直到脱战）、到期自动清理、单实例标签（同名替换）、payload 引用（源物品 UUID + 效果名 + 档位）、显示名、颜色、粒子样式（可选，默认关）。

生命周期：光环源激活（装备/运功/架招）→ 管理器创建 region + 行为；源消失 → 删 region，核心在 region 删除时为区域内每个 token 补发 `tokenExit`（`RegionDocument._onDeleteOperation` 已核实），已挂 AE 经门面正常摘除；行为另有 `behaviorActivated` / `behaviorDeactivated` 事件可用。exit 摘除按**贡献账本**执行（决策 22，第三轮修订取代原"跳过摘除"）：enter 记录叠加前后层数差为该光环的贡献数（存 region flags 轻量账本，按 token），exit 按贡献数摘除；非叠层 payload 记"是否本光环创建"，退出时仍被其他同 payload 光环覆盖则账本转移，否则删除；"每回合叠层、退出全清"类条目用行为 schema 的清空型选项。

### 3.2 范围与判定口径（2026-09-26 两轮拍板）

- 区域形状一律用 `grid` 类型（GridShapeData）：`offsets` 格子集合的成员判定、渲染、附着平移三者天然一致——**生效范围 = 可见范围 = 我们按 1-2-2-2 算出的格子集合**；`circle`/`emanation` 是欧氏几何圆，只配当视觉，不做生效判定。
- **单位（定案，决策 20）**：规则描述中的"米"即"格"，无任何换算、不读 `scene.grid.distance`；半径 N 米 = N 格，N 为非负整数。光环圆心一律吸附**格心**（从源 token 所在格中心按 1-2-2-2 公式算出覆盖格集合，复用 measured-template.mjs 保留的遍历算法；核心 Region 可锚格子顶点，这是我们的差异点）。附着跟随由核心整格平移 offsets。
- **进出判定直接采用核心包含语义**（决策 15，审阅修订）：核心 `testInsideRegion` 对 1×1 token 只测中心点（源码 `#getUnconstrainedTestPoints`："If at most 1x1, test center point"），即我们的中心格口径；多格 token 为足迹多测试点（任一命中即在内），按用户拍板接受核心口径、**不自研中心格状态机**。进出事件由核心派发，行为不做二次判定。
- **形状生成器**以圆形为主（约 99% 条目），支持矩形（3×1、3×3 等）与创建时按源 token 朝向的 90° 吸附旋转作为变体（剑气白云、刀气旋风）；多边形（星罗棋布棋子锚点）不支持，维持仅标记/手动。万剑归宗为**单 region + 大 offsets 集合**（剑痕触发不逐格消耗、湮灭一次性全清），不做逐格实例；区域内格间移动不重触发 `tokenEnter`，经 `tokenMoveWithin` 按**每次移动**结算一次（决策 23）。
- 无网格场景：grid 形状为空，系统直接禁用光环创建。**无实例查询（`queryTokens`）与 Region 判定同口径**：1×1 测中心、多格测足迹任一点（决策 35）。

### 3.3 UI 与创建路径

不自建配置页：行为 schema 字段由核心 RegionConfig / RegionBehaviorConfig 原生渲染编辑。三条创建路径汇入光环管理器：

1. **数据驱动**（主力）：装备/运功/进战时机自动创建，参数来自物品 payload 与光环约定，无 UI；
2. **快建工具**：工具栏按钮（`getSceneControlButtons` 注入已验证）→ 轻量弹窗（半径/持续/颜色/跟随）→ 画布格子吸附放置；旧 AOE Creator 交互骨架复活（S3.2/S3.4 并入本专项）；
3. **核心配置页细调**：双击光环 region 走核心 RegionConfig，形状/颜色/可见性/楼层沿用核心，我们的字段在 Behaviors 中编辑。

### 3.4 三种特殊实现形态

| 形态 | 写法 | 覆盖条目 |
|---|---|---|
| 监听 AE | 进入结算给范围内单位（含敌方）挂不可见监听 AE（`showIcon: 0`、无 changes），其脚本借**被挂者自身已有触发器**（attack/damaged/dying/death/turnStart）反向驱动持有者效果；离开撤销 | 原丁类 7 条（死人经、火浣功、狂沙·孤影、罗汉伏魔功、随风潜入夜、沙罗染血、贞观治世） |
| 脚本编排 | 现有触发器 + `token.regions` 反查 + 管理器实例 API（按标签查询/消耗/删除场上光环）：出招引爆、破招/格挡触发放置、碧火检定消耗、湮灭联动 | 10 条：原丙类脚本部分 9 条（暗刻、遁光×2、踏莲、天神断海、碧火×3、热恼地狱）+ 五雷真诀（attack 触发 + 区域查询替换手搓距离，决策 26 样板） |
| 脚本分发 | 进战脚本直接向所有友方分发 payload AE，不建区域 | 2 条：赤霄剑（原戊类）、马头琴·乘风（原文"所有友方"无半径，现脚本已符合） |

**横切需求**（不单独成类，逐条标注在清单中）：

- **分档联动**：半径或数值按招式等级/内功境界/消耗层数变化（浩渺气海 1/3/10 米、无涯三级、剑曲绝鸣三档、须弥按层数）。payload 已有按档位建多条 AE 的先例；动态数值有脚本先例（随风潜入夜动态改范围）。
- **单实例替换**："只能拥有一个 X，新的替换旧的"（箫音障、寒霜、无涯、灼日、魔气、旋风等）与计数上限（腐蛊雾霭 1+up、暗刻 4、天候互斥）。区域需要 slug 化的实例管理，对齐 AE 的 slug/叠层语义。
- **维持消耗**：回合末扣内力，不足则消散（青海 10、江南 1、冷心按人数×20）。脚本先例完整（青海 turnEnd 维持）。
- **生命周期绑定**：装备/运功/架招开启 → 创建；卸下/解除/被破除/死亡/脱战 → 销毁并清理已施加 AE。语义对齐 `tiedToStance` 的业务清理。
- **时限到期**：区域持续 N 回合后自动消失，对齐 V14 `duration` 模型与核心 registry（AE-05 已确立 `expiryAction: "delete"`）。
- **过滤条件**：阵营（友/敌/全体）、是否含自身（"其他友方"不含自身）、濒死、架招状态、黑子/白子等自定义条件。

## 4. 分类清单（94 条）

来源列只写到文件与条目名；"payload"列是物品级 effects 中已备的光环 AE（"—"为无 payload，需建或依赖系统状态）；"缺口"列引用第 5 节能力缺口编号；"现状"摘自数据 `automationNote`。

### 进入结算 · 跟随源（33 条，原甲-1；五雷、马头琴经第三轮审阅改归脚本编排/分发，见第 3.4 节）

| 名称 | 来源 | 光环机制 | payload | 标注/缺口 | 现状 |
|---|---|---|---|---|---|
| 铜铃猫项圈 | armor/necklace | 周围 10 米其他友方先攻+1 | —（需新建光环 AE） | 友方·不含自身 | 光环手动 |
| 浩渺气海 | neigong/fenghuayuan《气海真诀》 | 范围内敌人命中-10 | 浩渺气海 | 敌方·半径按境界 1/3/10 | 手动拖拽 |
| 浴㞎功 | neigong/jianghushili1 | 近身敌人陷"恶臭" | 恶臭 / 恶臭(圆) | 敌方·细节待澄清 | 施加自动，检测手动 |
| 炙阳心经 | neigong/jianghushili2 | 范围内敌人出招内力消耗+5/+10 | 炙阳真气(敌方) I/II | 敌方·两档 | 手动 |
| 天帝遗简 | neigong/jianghushili2 | 友方光环 | 天帝威仪(友方) | 友方·数值 0 占位待定 | 手动 |
| 青袍神功·青海 | neigong/jianghushili2 | 区域内所有人（含己）视作处于水中 | 水中战斗(领悟/小成/圆满) | 全体含己·维持消耗·三档 | 已脚本半自动 |
| 化无形书 | neigong/jianghushili3 | 进入区域者获"蓄劲/犹豫" | — | 全体·系统状态 | 手动（GM） |
| 易理天问 | neigong/jianghushili3 | 范围内队友双防/伤+5/+10 | 易理 (小成以下/圆满) | 友方·两档 | 手动拖拽 |
| 血刀经 | neigong/jianghushili3 | 敌方获"败血"（回合初回血-15） | 败血 | 敌方·圆满回血判定另丁类 | 部分自动 |
| 神女赋·湘水 | neigong/jianghushili4 | 范围内敌内防-10；命中联动已自动 | 湘水 | 敌方 | 手动 |
| 将星录·剿灭 | neigong/jiangjunying | 攻击者常驻闪避-10/看破-2 | 剿灭 | 范围与触发待澄清 | 士气自增自动，其余手动 |
| 风玄劲·大风域 | neigong/shenfengbang | 大风区域：困难地形+远程伤害修正 | 大风域 | G1 地形·G4 天候 | 手动 |
| 伏龙诀·伏龙威压 | neigong/wanshoushanzhuang | 进战范围内敌看破/虚招-2 | 伏龙威压 | 敌方 | 半自动（进战卡片提醒） |
| 惜余琴 | weapons/instrument | 进战后 10 米友方格挡+5 | 惜余琴（格挡+5） | 友方 | 部分自动（自身自动） |
| 保晋戈 | weapons/staff | 架招开启时周围 1 米友方格挡+10 | — | 友方·架招条件 | 手动 |
| 神打棍·请岁君·太岁当值 | wuxue/gunfa | 周围 3 米所有人（含己）各类检定与虚招对抗劣势 | 太岁当值（自体版） | 全体含己·需建他人版 | 自身劣势自动，光环手动 |
| 弹剑作歌·剑曲绝鸣 | wuxue/huashanpai | 气场内敌人出招内力消耗+1/+3/+5 | 剑曲绝鸣(敌方减益)×3 档 | 敌方·三档 | 半自动 |
| 无涯双剑·海天涛怒·无涯 | wuxue/jianfa | 跟随 3+up 米友方伤+5/10/15、回合末回内 5 | 无涯 (1/2/3 级) | 友方·三档·单实例·时限 | 手动 |
| 惊世剑法·海市蜃景·云气 | wuxue/jianfa | 范围内按龙气层闪避+3；真龙反应使攻击丢失 | — | 条件层数·反应事件 | 手动 |
| 覆雨神剑·潮起潮落·天候 | wuxue/jianfa | 20 米天候：命中/虚招/看破-（含己） | — | 全体含己·G4 天候·互斥 | 消耗与计时自动，光环手动 |
| 书香江南 | wuxue/jiangnange | 自身与 5 米友方伤+5；回合初 1 内力维持 | —（脚本构造） | 友方·维持 | 已脚本自动化 |
| 九宫八卦剑·六八为足·地宫 | wuxue/qinwangfu | 地宫 3+up 回合：友方速度+1；敌方困难地形 | — | 友方+G1 地形 | 禁足自动，区域手动 |
| 九宫八卦剑·二四为肩·天宫 | wuxue/qinwangfu | 天宫 3+up 回合：友方暴击骰-1；敌方闪避-5 | — | 双方 | 击倒自动，区域手动 |
| 连山六气掌·画碧染绿 | wuxue/tushou | 3 米友方按六气六选一增益 | — | 友方·六选一互斥 | 纯手动 |
| 连山六气掌·九紫运来 | wuxue/tushou | 10 米六气全效果翻倍（六丁六甲阵） | — | 友方·依赖画碧染绿 | 纯手动 |
| 九鼎步·阴阳所化·阴阳变 | wuxue/tushou | 5 米内所有目标武学属性阴阳倒转 | — | 全体·G5 特殊规则 | 消耗自动，倒转手动 |
| 须弥神掌·须弥镇邪·小须弥天 | wuxue/wanfosi | 区域内敌下盘不稳+招式距离/速度-X | 须弥（层数资源） | 敌方·按消耗层数 | 伤害自动，区域手动 |
| 逍遥掌法·势若垂天 | wuxue/xiaoyaopai | 踏入 3 米内敌人招式伤害降低 10+5up | —（垂天自体 buff 脚本构造） | 敌方·G3 位移（击退另计） | buff 自动，光环手动 |
| 星罗棋布·大飞守角 | wuxue/xiaoyaopai | 与两白子三角区域友方格挡+10+5up；黑子无效 | — | 友方·G6 特殊形状·阵营条件 | 手动 |
| 星罗棋布·活气做眼 | wuxue/xiaoyaopai | 白子相连区域：友方命中优势暴击骰-1；黑子劣势 | — | 双方·G6 特殊形状 | 手动 |
| 琵琶行·千呼万唤·荻花秋瑟 | wuxue/yueqi | 格挡触发 3+up 米敌速度减半+攻击距离-1 | — | 敌方·时限 | 提示，光环手动 |
| 横渠四剑·为生民立命 | wuxue/zhengqizong | 4 米友方濒死不暴击+双防 10×等级 | 生民立命 | 友方·G5 特殊规则·等级联动 | 手动 |
| 横渠四剑·为万世开太平 | wuxue/zhengqizong | 1+up 米敌人无法获得怒气 | 万世开太平 | 敌方·G5 特殊规则 | 手动 |

### 进入结算 · 放置（10 条，原甲-2）

| 名称 | 来源 | 光环机制 | payload | 标注/缺口 | 现状 |
|---|---|---|---|---|---|
| 恶临破空功·黑幕 | neigong/jianghushili3 | 大范围黑幕区域，区域内角色获状态 | 黑幕 | G2 视线 | 手动绘制+赋状态 |
| 风云双刀·旋风 | weapons/blade | 原地 1 米旋风 1 回合：区域内自身格挡+5 | — | 自体·单实例 | 手动 |
| 冰雪刀法·了因现雪·寒霜 | wuxue/daofa | 5 米寒霜 1 回合：全体外防+5 内防-5（升级再各+5） | — | 全体·单实例 | 半自动（提示） |
| 拭剑听风·听风 | wuxue/huashanpai | 选空格 3 米听风 3 回合：友方伤+5+5up | — | 友方·单实例 | 半自动（提示） |
| 冷血剑法·月影碎空·月影 | wuxue/liushanmen | 原地 3 米月影：自身格挡+5 看破+1 | 月影 AE | 自体·单实例 | 部分自动 |
| 雾锁腐蛊·腐蛊雾霭 | wuxue/other | 目标空格 3 米 3 回合：命中/虚招劣势；离开眩晕 1 回合 | — | 全体·离开惩罚·数量上限 1+up | 手动 |
| 玄蛇索魂·地蟒翻身·尘沙 | wuxue/other2 | 周围 3 米尘沙 3 回合：全体目盲 | — | 全体 | 提示，目盲手动 |
| 烈云神箭·幻雾蔽日·尘埃/乱雾 | wuxue/qimen | 架招+2 米尘埃：全体目盲；格挡成功转乱雾（不可被选中） | — | 全体·状态转换·G2 目标 | 提示，区域手动 |
| 罗汉棍法·恒河结沙 | wuxue/wanfosi | 周围 1+up 米沙尘 1 回合：目盲+可用【普度四方】替代《趁虚而入》 | — | 全体·G5 特殊规则（招式替换） | 提示 |
| 衡山五峰剑·天柱剑·听风 | wuxue/wuyueqingcheng | 2 米听风 2+up 回合：友方伤+5 | — | 友方·单实例 | 手动 |

### 回合结算 · 跟随源（9 条，原乙-1；可与进入结算同开）

| 名称 | 来源 | 光环机制 | payload | 标注/缺口 | 现状 |
|---|---|---|---|---|---|
| 清辉华盖伞 | weapons/special | 每回合初给 1 米友方+5 护体真气 | — | 友方·tick 治疗 | 手动 |
| 雪饮六诀·透骨冷意 | wuxue/daofa | 范围内敌人也陷入"冷心"；回合初按冷心人数×20 耗内力维持 | 冷心 | 敌方·状态传染·维持联动 | 冷心自身自动，光环手动 |
| 烟雨剑法·春雨绵绵 | wuxue/jiangnange | 架招期间回合初为 3 米未濒死友方回 5 气血 | — | 友方·濒死过滤·tick 治疗 | 回合初卡片提示 |
| 金乌刀法·骄阳似火·灼日 | wuxue/qinwangfu | 跟随 10 米：敌回合初 10 火伤，无架招则目盲 | — | 敌方·终止条件（解除架招） | 提示，结算手动 |
| 无形神音·无音 | wuxue/sanshou | 1 米内敌回合初流失 30+10×通音等级 | — | 敌方·被动无动作·跨招式联动 | 半自动 |
| 蜚蠊掌·掌蠊天地·蜚蠊地狱 | wuxue/tushou | 蓄力扩展范围：敌回合初 20 毒伤+定力 DC5 眩晕 | — | 敌方·范围成长 | 手动 |
| 心魔连环掌·魔气纵横 | wuxue/tushou | 跟随 1 米：敌回合初 20 毒伤；困难地形+禁轻功 | — | 敌方·G1 地形·特殊终止条件 | 手动 |
| 百兽绘卷·狼入羊群·走兽 | wuxue/wanshoushanzhuang | 回合结束时周围敌人流失 10 气血 | — | 敌方·G7 路径（移动释放） | 回合末手动 |
| 折杨柳·是为相思·相思 | wuxue/yueqi | 3 米内敌回合初定力 DC16，失败 10 精神伤；"惜别"必败 | 相思 | 敌方·检定 tick | 获状态自动，tick 手动 |

### 回合结算 · 放置（12 条，原乙-2；可与进入结算同开）

| 名称 | 来源 | 光环机制 | payload | 标注/缺口 | 现状 |
|---|---|---|---|---|---|
| 乾坤尺法·尺定乾坤·乾坤气场 | wuxue/bishou | 格挡触发 5 米气场 2 回合：敌回合初流失 5 气血 | — | 敌方·单实例 | 激活自动，结算手动 |
| 玉箫剑诀·客舟何处·箫音障 | wuxue/bixiaoshishen | 格挡触发 1+up 米 2 回合：敌回合初 10 精神伤 | — | 敌方·单实例 | 提示，结算手动 |
| 神雷九刀·天谴雷罚·雷电真龙 | wuxue/daofa | 10 米 1+雷霆回合：开始回合投 2D6 查随机表（敌/友表不同） | — | 双方·随机表·延时消耗 | 提示，结算手动 |
| 神宵矛法·五雷轰顶·雷云 | wuxue/gunfa | 5 米 3 回合：敌回合初流失五雷总层数×10；视作雷暴天气 | — | 敌方·G4 天候·联动消耗 | 层数统计自动，结算手动 |
| 明月剑法·白云一片·剑气白云 | wuxue/jianfa | 面前 3×1 米 2 回合：开始回合或进入受 0.4 内息内功伤（必中无暴击） | — | 全体·矩形区域 | 提示，结算手动 |
| 明月剑法·斜月沉沉·雾海 | wuxue/jianfa | 层数²半径至脱战：敌耗内力受伤；困难地形+重度遮蔽+攻击限制 | — | 敌方·G1 地形·G2 视线 | 手动 |
| 神风刀法·神刀风暴·刀气旋风 | wuxue/shenfengbang | 正前 3×3 米 1+up 回合：开始回合 40 流失；视作大风天气；刀气延时 | — | 全体·矩形·G4 天候·联动延时 | 提示，结算手动 |
| 洛神赋·洛神之怒·水牢 | wuxue/tushou | 目标+2 米 3+up 回合：开始回合或首进 50 流失（必中无暴击）；困难地形 | — | 敌方·G1 地形·首进触发 | 手动 |
| 遮天蔽日掌·乌云盖顶·黑烟 | wuxue/tushou | 目标空格至脱战：无法暴击+回合初 20 毒伤 | — | 敌方·多实例·G5 特殊规则 | 提示，生成手动 |
| 遮天蔽日掌·黑云压城·黑烟 | wuxue/tushou | 看破反应+目标空格生成黑烟 | 同上 | 同上·反应触发 | 提示 |
| 遮天蔽日掌·乌烟瘴气·黑烟 | wuxue/tushou | 架招格挡成功在目标空格生成黑烟 | 同上 | 同上·格挡触发 | 提示 |
| 遮天蔽日掌·蒙心绝念 | wuxue/tushou | 每吸纳一团黑烟伤+10；≥7 团则范围内每空格生成黑烟 | — | 敌方·实例计数联动 | 计数手动输入 |

### 原丙类 · 触发/陷阱/特殊实例（11 条，已折叠）

> **折叠归属（2026-09-26）**：剑痕→进入结算（踏入/回合双触发）+脚本编排（湮灭联动）；箭雨→进入结算（每回合限一次）+回合结算；暗刻→脚本编排（attack 触发引爆 + 实例消耗）；遁光×2（光影荏苒/望风扑影）→脚本编排（damaged 触发放置）；踏莲→脚本编排（首进触发；移动路径留痕受 G7 限制，先手动放置）；天神断海→脚本编排（禁足部分受 G3 限制手动）；碧火照天/绿蛇火舞/碧焰追魂→脚本编排（放置 + 事件触发 + 检定消耗 + 实例联动）；热恼地狱→进入结算挂 flag AE + 招式脚本联动（G5）。

| 名称 | 来源 | 光环机制 | payload | 标注/缺口 | 现状 |
|---|---|---|---|---|---|
| 万剑诀·万剑归宗·剑痕 | wuxue/jianfa | 20 米每个空格布剑痕：开始回合或踏入流失 10；可湮灭结算伤害+封招+返还剑气 | — | 多实例（网格级）·联动招式 | 预检与提醒自动 |
| 漫天箭雨·箭雨 | wuxue/jiangjunying | 目标空格+3 米 3 回合：每回合首次进入/开始回合流失+流血 1 层；按箭矢品级加成 | — | 首进触发·品级联动 | 卡片指引手动 |
| 对对胡·暗刻四藏·暗刻 | wuxue/luoshengongjia | 4 格暗刻：敌人立于其上出招即引爆 0.4 力量伤+击退 1 米 | — | 4 实例·陷阱·G3 位移 | 手动 |
| 光影刺·光影荏苒·遁光 | wuxue/mingjiao | 被破招时 3 米遁光至脱战：敌目盲+下盘不稳+受击伤+10 | 蕴影（关联） | 事件触发放置·单实例 | 提醒 |
| 光影刺·望风扑影·蕴影 | wuxue/mingjiao | 蕴影期间格挡成功在 1 米生成遁光 | 蕴影 | 事件触发放置 | 蕴影自动，遁光手动 |
| 踏歌舞·步步生莲·踏莲 | wuxue/tushou | 移动路径留踏莲 1 回合：首进敌禁足 1 回合/友回血 10 | — | 路径触发·G7 路径·首进 | 手动 |
| 登云八步·天神断海 | wuxue/tushou | 选 3×3：速度低于自身者禁足至下回合开始；下回合起跳对区域内伤害 | — | 两阶段·G3 位移·条件比较 | 纯手动 |
| 摘星碧火手·碧火照天·碧火 | wuxue/xiaoyaopai | 选空格燃碧火至脱战：站上火上下[角力/巧手]检定扑灭 | — | 放置·检定交互·多实例 | 手动（Token/Tile） |
| 摘星碧火手·绿蛇火舞·碧火 | wuxue/xiaoyaopai | 击破架招在目标格燃碧火 | — | 事件触发放置 | 提示 |
| 摘星碧火手·碧焰追魂 | wuxue/xiaoyaopai | 消耗一团碧火化为火球射出（或改为直接爆炸+50） | — | 实例消耗联动 | 手动 |
| 阎王棍·泰山锥肉·热恼地狱 | wuxue/yanwangmiaomiao | 区域内【大热恼】免"行刑"压制反击；【阿鼻降难】格挡附"行刑" | — | G5 特殊规则（招式联动） | 提示+计数 |

### 原丁类 · 事件检测型（9 条 → 监听 AE 形态，零新增触发器）

> **归属（2026-09-26）**：监听 AE 形态 7 条——死人经（death）、沙罗染血（attack/dying）、贞观治世（damaged）、随风潜入夜（**dying 映射残疾检定**：数据作者确认残疾检定由濒死触发，用现成濒死触发即可，映射口径为"每次进入濒死计一次"）、火浣功（自身 damaged）、狂沙·孤影（自身 turnStart 查周围友方）、罗汉伏魔功（现有半自动维持）；保持手动 2 条——罗汉棍阵、飞升（借机窗口，规则为"移动时"，无对应触发器，用户拍板不做近似实现）。

| 名称 | 来源 | 光环机制 | payload | 标注/缺口 | 现状 |
|---|---|---|---|---|---|
| 死人经·死气 | neigong/jianghushili1 | 周围 10 米有人死亡→获得一层死气（至多 10） | — | 死亡事件 | 手动 |
| 狂沙神功·狂沙·孤影 | neigong/jianghushili1 | 条件防御增益（疑为"周围无友方"时生效） | 狂沙·孤影(小/圆) | 触发条件待澄清 | 手动 |
| 火浣功 | neigong/jianghushili1 | 受击时自体爆炸 AOE | — | 受击事件·范围伤害 | 提示手动 |
| 罗汉伏魔功·震伤 | neigong/wanfosi | 出招后范围震伤（范围细节待澄清） | — | 出招事件 | 卡片提示 |
| 春雨润物诀·随风潜入夜 | wuxue/baicaoge | 架招期间 5+5up 米友方残疾检定时+1 怒气 | 随风潜入夜 | 检定事件·动态范围 | 提示 |
| 御魔式·沙罗染血 | wuxue/mingguiqianmen | 1+up 米内他人出招回 5 内；敌人濒死回 1 怒 5 血 | 沙罗染血（自体） | 出招/濒死事件 | 状态自动，光环手动 |
| 圣世龙拳·贞观治世·盛世 | wuxue/tushou | 范围内有角色受伤→消耗反应减伤（锦鲤层数×30） | — | 受伤事件·反应消耗 | buff 自动，反应手动 |
| 罗汉棍法·罗汉棍阵 | wuxue/wanfosi | 3 米内敌人移动时可将施展《趁虚而入》（借机窗口） | —（脚本构造 buff） | 移动事件·借机窗口 | buff 自动，借机手动 |
| 如来神掌·飞升 | wuxue/wanfosi | 漂浮+普攻+10 米；10 米内敌人移动时免费《趁虚而入》 | 飞升 | 移动事件·借机·G8 半空 | buff 自动，借机手动 |

### 原戊类 · 全局光环（1 条，已除名 → 脚本分发）

> 赤霄剑不需要区域：进战脚本向所有友方分发 payload AE（马头琴先例），不占光环框架。

| 名称 | 来源 | 光环机制 | payload | 标注/缺口 | 现状 |
|---|---|---|---|---|---|
| 赤霄剑 | weapons/sword | 装备后所有友方（含己）命中、招式伤害+5 | 赤霄剑-帝道光环 | 无距离限制 | 自身自动，队友手动 |

### 原己类 · 规则型（6 条 → 仅标记形态）

> 只画范围（结算模式"仅标记"）+ 创建时发一次说明卡（写明哪些部分由 GM 手动结算）+ 区域显示名；"回合提醒"做成默认关闭的开关。

区域可视化与源生命周期可并入甲/乙类框架，但"机制"列的规则部分 payload AE 无法表达，需专项或长期手动。

| 名称 | 来源 | 机制（无法 AE 化的部分） | payload | 标注/缺口 | 现状 |
|---|---|---|---|---|---|
| 五行斧诀·赤火焚天 | wuxue/daofa | 5 米内想远离者视作困难地形移动 | — | 移动意图判定 | 手动 |
| 弈理指归·弈理天问·渊微 | wuxue/jiangnange | 区域内敌人无法离开；自身回合初额外主要动作 | 弈理（资源） | 移动限制 | 弈理检查消耗自动 |
| 剑嶽临渊·剑嶽 | wuxue/qingtianmen | 区域内敌困难地形；范围外敌每回合末被拉向中心 1+up 米 | 剑嶽 | G1 地形+G3 位移 | 状态与普攻选项自动 |
| 大风起·大风 | wuxue/shenfengbang | 3+up 米 10 回合：困难地形+远程投射伤害修正（敌-10/我+10） | — | G1 地形+G4 天候 | 纯手动 |
| 江山如画·江河入海·江河 | wuxue/zhengqizong | 1 米内结束回合的目标被反向击退 1 米 | 江河（自体） | G3 位移 | 获状态自动，击退手动 |
| 太极拳·如封似闭·炁化太虚 | wuxue/zhenwujiao | 范围外敌方无法选中范围内友方 | — | G2 目标选取 | 提示 |

### 待澄清（1 条）

| 名称 | 来源 | 说明 | payload | 现状 |
|---|---|---|---|---|
| 万毒心经 | neigong/jianghushili2 | 打了光环标记但描述仅风味文本；毒血储存为手动记录。需对照规则书确认其光环面 | 毒血 | 完全手动 |

## 5. 能力缺口汇总

清单"缺口"列引用以下编号。G0 能力（进出施加、回合结算、放置与时限、事件检测）是各分类的主实现；G1～G8 是 payload AE 与 Region 事件表达不了的规则面。

| 编号 | 缺口 | 涉及条目 |
|---|---|---|
| G1 | 困难地形/移动消耗 | 大风域、赤火焚天、雾海、剑嶽、地宫（敌侧）、大风、魔气、水牢 |
| G2 | 视线遮蔽/目标选取规则 | 黑幕、雾海（重度遮蔽）、乱雾（不可选中）、炁化太虚（保护）、恒河结沙（无法视物，目盲状态部分可自动化） |
| G3 | 强制位移（击退/拉扯/位移判定） | 剑嶽拉扯、江河击退、势若垂天击退、暗刻击退、天神断海禁足 |
| G4 | 天候系统（天气标记、投射物修正、天候互斥） | 雷暴（雷云）、大风（×3）、覆雨神剑天候、神刀风暴 |
| G5 | 引擎 flag 类规则（无法暴击、怒气封锁、濒死不暴击、属性倒转、招式替换/免压制、阵营条件） | 黑烟×4、为生民立命、为万世开太平、阴阳变、恒河结沙、热恼地狱、星罗棋布（黑子/白子） |
| G6 | 特殊区域形状（棋子锚点多边形） | 大飞守角、活气做眼 |
| G7 | 移动路径触发（路径伤害/路径留痕） | 踏莲、走兽路径伤害、步步生莲/狼入羊群移动释放 |
| G8 | 高度/半空判定 | 飞升（漂浮）、白云一片（对半空目标）、黑云压城（半空释放） |

> G5 的多数条目（如"无法暴击""怒气封锁"）可以走"payload AE 写 flag + 引擎相应结算点读 flag"的自研路线，工作量在引擎结算点而非区域框架；G1/G2/G3/G4 涉及移动与视线规则，V14 核心无地形系统，建议列入暂缓并保持手动；G6/G7/G8 属长尾，先标记不设计。

**G5 补充（2026-09-26 第六轮修订，源码核实）**：三项**全部零引擎改动**——**怒气封锁**：`noRecoverRage` 读取链路已存在于 `actor.mjs` 的 `blockRecovery`，"万世开太平"payload 已写该 flag；**濒死免暴击**："生民立命"payload 现有的 `preDefense` 受击脚本即实现（受击时判定自身处于濒死 → `args.config.isCrit = false`，取消本次暴击伤害倍率），无状态、无 flag、无生命周期管理；**黑烟无法暴击**：新建公共"黑烟"AE 挂出招脚本大幅负向修正 `critThresholdMod`（该值正数更容易暴击、负数更难，item.mjs 注释明示），使暴击阈值不可达。已知边界：濒死免暴击仅取消伤害倍率，战报仍显示暴击、isCrit 消费方仍见暴击状态；黑烟的脚本强制暴击（forceCrit）可绕过阈值修正。属性倒转（阴阳变）、招式替换（恒河结沙）、免压制（热恼地狱）三项改动大收益低，**延后并维持手动**（2026-09-26 用户确认，决策 19）。

**G1 补充（2026-09-26）**：核心 `modifyMovementCost` 行为走原生地形难度管线（`_getTerrainEffects` → 移动路径 terrain cost）；数据作者记忆 V13 时期困难地形与 1-2-2-2 计费可叠加。V14 下两者的叠加关系列为实现期实测项（见第 6 节第 13 条），实测通过可为仅标记光环补"地形成本"开关，未通过则维持手动；不进本轮承诺。

## 6. 决策记录（2026-09-26，与数据作者讨论定稿）

1. **Q4 架构方向定稿：完整光环系统（方案 B）**——自定义行为类型 + 光环管理器；核心 `applyActiveEffect` 行为（方案 A）否决：它走裸 `createEmbeddedDocuments` 不经过门面，丢 slug/叠层/权限代理/飘字语义。
2. **一个行为类型承载全部结算**：进入结算/回合结算/仅标记三开关独立可组合；进入结算动作支持挂 payload AE、直接伤害治疗、每回合限一次节流。
3. **范围判定**：grid 形状（GridShapeData）+ 1-2-2-2 offsets，token 中心格判定，光环中心吸附源 token 所在格中心；无网格场景禁用。
4. **UI**：不自建配置页，行为字段经 schema 由核心 RegionConfig/RegionBehaviorConfig 原生渲染；三条创建路径（数据驱动/快建工具/核心页细调）；快建工具复活旧 AOE Creator 骨架，S3.2/S3.4 并入本专项。
5. **事件执行统一 GM**：进出事件虽在移动发起者客户端派发，行为 handler 一律经 socketlib 委托活动 GM 结算（当前端即 GM 时直接执行，避免无谓往返）——用户拍板"统一执行"。
6. **战斗外不结算**（用户拍板）：回合事件只在战斗内派发，战斗外光环仅保持区域存在与进出挂摘；乙类条目战斗外沿用手动。
7. **粒子为可选项**（用户拍板）：默认关，性能优先；区域本体 `visibility: ALWAYS` 纯色填充兜底。V14 原生 ParticleGenerator/VFX 系统与 anime.js ^4.3.6 已核实存在，供可选启用。
8. **原丁类定为监听 AE 形态，零新增脚本触发器**：7 条用现有触发器（death/dying/attack/damaged/turnStart）；随风潜入夜的"残疾检定"映射为濒死触发（dying）——数据作者确认残疾检定由濒死触发，不新增触发点（用户决策 2026-09-26）；借机窗口 2 条（罗汉棍阵、飞升）保持手动（用户拍板）。
9. **原己类定为仅标记形态**：只画范围 + 创建时一次说明卡 + 区域显示名；回合提醒默认关闭；G1 地形成本为 `modifyMovementCost` 实测通过后的可选后门，不进本轮承诺。
10. **原戊类除名**：赤霄剑按脚本分发实现，不占光环框架。
11. **S4.3 并入**：跟随光环宏的 Region 版改写与快建工具是同一件事，不另设入口。
12. **万毒心经维持待澄清**：规则书核对前按手动处理，核对后回填分类。
13. **进计划验收矩阵的待实测清单**：
    ① 裸 update 传送不触发进出的对账兜底（回合事件时补扫或 updateToken 钩子）；
    ② 非链接 Token 合成 Actor 的施加/清理分支；
    ③ region 删除补发 `tokenExit` 与门面清理的执行顺序；
    ④ 万剑归宗 20 米全格 offsets（921 格）的渲染与判定性能；
    ⑤ `modifyMovementCost` × 1-2-2-2 measurePath 劫持的叠加；
    ⑥ 多光环并存（10+ region/场景）的帧率摸底（粒子开关阈值依据）；
    ⑦ 濒死/死亡时点脚本是否仍随 Actor 正常执行（监听 AE 依赖）；
    ⑧ 1×1 token 核心包含判定的运行时复验（源码已核实单点中心，见第 3.2 节）；
    ⑨ 核心回合事件（fire-and-forget 已从源码确认）与我方 EncounterManager 回合队列的实际交错（幂等设计依据）；
    ⑩ 矩形形状与按源朝向 90° 吸附旋转 offsets 的正确性，及 `tokenMoveWithin` 事件携带的 movement 数据形态与触发粒度（决策 23"每移动结算一次"的依据）。

    **销项记录（2026-09-26，AURA-01 spike，14.368 实测）**：③④⑤⑥⑧⑨⑩ 已实测销项；AURA-01 工作项中的玩家移动派发端与 isSelf 门控已实测，socketlib 委托 GM 全链路与竞态转 AURA-03 验收；① 的"裸传送不派发事件"前提已被决策 39 修正（对账兜底保留为保险层）；②⑦ 维持原归属（②随 AURA-03/05 验收、⑦濒死脚本归 AURA-10）。逐项结论见第 7.2 节 AURA-01 条目。

**第二轮修订决策（2026-09-26，外部审阅后与数据作者确认，14～21）**：

14. **多来源清理简化**：enter 不做检测、正常走门面叠层/刷新语义；exit 仅加轻量防误删（`token.regions` 反查同 payload 覆盖，在则跳过摘除）。不做所有权账本与转移；"同 payload 双光环重叠退出其一"与"目标预存同名 AE 再进出"两个极端组合接受为已知边界，样板闸门（AURA-08）以用例覆盖其行为符合预期。（**第三轮修订 22 取代本条的"跳过摘除"与"无账本"表述**——跳过摘除会留下幽灵叠层，账本方案同时修复该问题与预存 AE 边界。）
15. **进出判定采用核心包含语义**：1×1 token 即中心点判定（源码核实，与格成员判定等价）；多格 token 接受足迹多测试点口径；不自研中心格状态机、不做二次判定。
16. **来源生命周期**：销毁时机按效果原文——架招光环关架招/被破/濒死即毁（经既有 `stopStance` 路径直调管理器）、装备光环卸下即毁、运功光环切换即毁。实现选**系统级 Foundry 钩子**（`updateItem`/`deleteItem`/`updateActor`，钩子内查源物品映射、无命中零开销返回）而非新增脚本触发器，不破坏决策 8；region flags 持久化源物品 uuid、到期轮次、维持状态与战斗 ID，ready 时恢复计时与校验。
17. **结算幂等**：核心回合事件为 fire-and-forget 且与我方回合队列并发（源码确认），不规定全局顺序（沿 Q5 口径）；要求每个结算幂等——同一单位同一回合不重复扣血/扣维持，维持消耗**单一归属管理器源侧**（区域行为不扣钱），从机制上杜绝双扣。
18. **对账必选**：`updateToken` 钩子去抖对账为必选项（战斗外传送没有回合事件，仅靠回合对账永远不会补正），回合事件对账降为补充。
19. **G5 范围**：属性倒转/招式替换/免压制延后维持手动；无法暴击/濒死不暴击/怒气封锁的实现方式经第三轮修订为状态 flag 抑制路径（决策 24，其中怒气封锁仍为零改动）。
20. **单位定案**：规则"米"=格，半径按非负整数格存储，无任何换算、不读场景 grid 设置。
21. **实施优先级**：框架先行，94 条内容自动化是最后一步；逐条评估自动化成本，能用框架与现成机制表达的全自动，超出框架能力的条目降级为"提示卡片 + 手动"，不强行实现，并在 automationNote 与第 4 节清单如实标注。

**第三轮修订决策（2026-09-26，第二轮审阅后与数据作者确认，22～26）**：

22. **叠层摘除 = 贡献账本（取代决策 14 的"跳过摘除"）**：可叠层 payload 的语义为"每个光环贡献自己的层数，退出减去自己贡献的层数"——enter 时记录叠加前后层数差（受 maxStacks 钳制时记 0）为贡献数，存于 region flags 的轻量账本（按 token）；exit 按贡献数 `removeEffect`。账本同时正确处理：双光环重叠（进 A→1 层、进 B→2 层、退 A→1 层、退 B→整条移除）、预存同名 AE（进 +1/退 -1，预存层数保留）、满层钳制（进 0/退 0，无漂移）。非叠层 payload 账本记"是否本光环创建"：退出时仍被其他同 payload 光环覆盖则账本转移给存活光环，否则删除。另有**清空型**语义（每回合叠层、退出全清）作为行为 schema 的退出摘除选项，由数据按条目选用。AURA-05 对账基准相应为"覆盖状态 ↔ 施加账本"。（**第五轮修订 32：分档重叠不做重算，同 payload 不同档位重叠且高档先退时的数值残留记为已知边界。**）
23. **万剑归宗格内移动**：区域内格间移动不重触发 `tokenEnter`，行为订阅 `tokenMoveWithin` 按**每次移动**结算一次（2026-09-26 用户拍板"每移动就扣一次"）；spike 实测事件 movement 数据形态与触发粒度。
24. ~~暴击状态抑制路径~~（**第六轮修订 33 整项取代：不加任何暴击 flag 与引擎读取点——濒死免暴击由"生民立命"现有 preDefense 受击脚本实现，黑烟由新建 AE 的出招脚本负向修正暴击阈值**——见第六轮决策块。）
25. **战斗中换装边界**：光环创建由数据脚本时机驱动，战斗中新装备不自动创建光环，在 automationNote 与自动化说明中讲清（2026-09-26 用户拍板）。EQUIP 触发器派发本身不大（约十行 + 注册与文档）但每件装备需成对维护 equip/combatStart 脚本，列为后续候选（第 7.5 节），本轮不做。
26. **手搓距离计算迁移（后续候选）**：五雷的 attack 脚本改用管理器区域查询替换 Math.hypot 粗算作为样板；data/ 中同类"出招给范围内单位上效果"的脚本后续可逐批迁移到区域查询，获得 1-2-2-2 口径与可视化（2026-09-26 用户指示记录，本轮只改五雷）。

**第四轮修订决策（2026-09-26，第三轮审阅后确认，27～30）**：

27. ~~濒死免暴击条件化~~（**第五轮修订 31 取代：不加 `noCritImmuneDying` 专用 flag，仅保留通用 `noCrit`/`noCritImmune` 两键，濒死条件由数据侧脚本切换**——见第五轮决策块。）
28. **在途竞态补偿**："执行前重查"不覆盖施加在途的窗口（enter 重查通过后异步写入期间 token 已退出、exit 见账本为空而结束，最终区域外残留 AE）——异步结算**写入完成后二次核对覆盖状态**，token 不在覆盖内或 region 已删除则立即按账本补偿清理；用例：快速进出、Region 删除时施加仍在途。（**第七轮修订 34 补充：二次核对仍不解决"进→出→再进"交错——账本操作改为按目标＋payload 串行队列、执行回合核对，见 34。**）
29. ~~分档重叠的退出恢复~~（**第五轮修订 32 取代：分档重叠属 99% 用不到的场景，不做账本记档与退出重算，接受已知边界**——见第五轮决策块。）
30. **无实例范围查询 API**：五雷为 attack 触发、不建常驻 Region，`query(label)` 只查已有实例不够——管理器新增 `queryTokens(source, {radius 或 offsets, 阵营过滤等})`：按 AURA-02 生成器现算 offsets、对候选 token 做中心格判定，返回 Actor 列表，不创建任何文档；五雷 attack 脚本调用该 API（决策 26 落地依赖）。（**第七轮修订 35：包含口径与 Region 一致——1×1 测中心、多格测足迹任一点（getContainmentTestPoints），避免漏掉身体已进入而中心未进入的大型目标。**）

**第五轮修订决策（2026-09-26，第四轮审阅后与数据作者确认，31～32）**：

31. ~~暴击 flag 通用化~~（**第六轮修订 33 进一步取代：set/clear 生命周期本身有洞——光环施加于已濒死者时 dying 触发不会再次发生，且受击脚本本就能无状态解决，flag 根本用不上**——见第六轮决策块。）
32. **分档重叠不做退出重算**：99% 用不到的场景不引入账本记档与重算机制——非叠层账本维持"是否本光环创建 + 覆盖时转移"的简单语义（决策 22），已知边界：同 payload 不同档位双光环重叠且高档先退时，目标在低档光环存续期间保留高档数值，至最后一个光环退出移除（2026-09-26 用户拍板"回到最优雅最简单的实现"）。

**第六轮修订决策（2026-09-26，用户复核决策 31 后确认，33）**：

33. **暴击类效果零 flag、零引擎改动**：状态切换式方案（dying 置 flag / 回血清除）存在生命周期漏洞——光环施加于已濒死的角色时 `dying` 触发不会再次发生，置位时序不可靠——而受击脚本本就能无状态解决：**濒死免暴击**由"生民立命"payload 现有 `preDefense` 脚本实现（受击时判定濒死 → `args.config.isCrit = false`，取消本次暴击伤害倍率），不删不改；**黑烟无法暴击**由新建公共"黑烟"AE 的出招脚本大幅负向修正 `critThresholdMod`（正数更容易暴击、负数更难，item.mjs 注释明示），使暴击阈值不可达。已知边界：①濒死免暴击仅取消伤害倍率，战报暴击显示与 isCrit 消费方仍见暴击状态；②黑烟的脚本强制暴击（forceCrit）可绕过阈值修正。（**第七轮修订 36 补充边界：手动伤害入口可直传 isCrit（`_handleManualDamage`），同样绕过阈值脚本，与 forceCrit 一并写入已知边界与黑烟 automationNote。**）`noCrit`/`noCritImmune` 引擎状态键取消，G5 三项全部零引擎改动（2026-09-26 用户拍板"受击脚本判断 hp<=0，flag 根本用不上"）。

**第七轮修订决策（2026-09-26，第五轮审阅后确认，34～36）**：

34. **账本操作按目标＋payload 串行**："执行前重查 + 写入后二次核对"仍不解决交错——enter#1 在途时目标退出又进入，两次 enter 完成时都看到"在范围内"，会重复施加且贡献记重（两次是不同事件，"同一事件幂等"约束不覆盖）。改为：账本操作按**目标 Actor ＋ payload** 串行队列执行，操作在其执行回合核对覆盖状态（入队前重查仅作快速过滤）；用例：进→出→再进、双光环同时进入。（**第八轮修订 37 补充：串行队列内 enter 以账本存在性幂等——同一 Region＋Token 已有有效账本条目时 enter 不再施加，防重复 enter 事件各加一层。**）
35. **queryTokens 与 Region 同口径**：无实例查询对多格 token 使用足迹测试点（任一点命中即在内，`getContainmentTestPoints`），与 Region 判定一致，避免五雷漏掉"身体已进入、中心未进入"的大型目标；验收补多格目标用例。（**第八轮修订补充：实现须显式传入文档源位置与尺寸调用 getContainmentTestPoints——无参调用取 prepared 值可能处于动画中，而 Region 判定使用 source 值。**）
36. **黑烟手动暴击边界**：手动伤害入口可直传 isCrit 绕过阈值脚本——与 forceCrit 一并记入已知边界，黑烟条目 automationNote 写明。

**第八轮修订决策（2026-09-26，第七轮审阅后确认，37～38）**：

37. **enter 施加以账本存在性幂等**：串行队列内逐条重放事件仍可能重复施加——同一 Region＋Token 的重复 enter 事件（区域边界重算、行为重激活等引起）各自通过"在范围内"检查。改为：**每次成功处理的 enter 记录独立账本条目 `{active: true, contributedStacks, created}`**；enter 执行时该 Region＋Token 已存在条目即不再施加、直接视为已生效；exit 按条目摘除后清除账目；"按当前覆盖与账本差异对账"为其等价形式，取实现更简者。（**第九轮修订细化：`active` 是唯一的幂等与对账依据——贡献层数为 0（叠层已满）或 created 为 false（AE 已由其他来源创建）的 enter 同样记 `active: true` 条目；若以"贡献>0 或创建标记"作为有效性判据，这两种已处理的 enter 会被重复施加，触发门面的时长刷新/数值覆盖（facade 有更新行为，源码核实）。**）
38. **账本闸门用例修正**：预存层数用例原为空操作——maxStacks:3 下预存 3 层时 enter 贡献恒为 0，即使预存保护失效用例也通过——改为"预存 1 层→进入变 2 层→退出回 1 层"，满层钳制独立用例（预存满 3 层，进退均为 3 层）；测试 payload 须置于**临时物品的 effects 中**，经"源物品 UUID＋效果名"解析路径施加，覆盖 AURA-03 的 payload 引用契约。

**机制修正决策（2026-09-26，AURA-01 spike 实测，39）**：

39. **S0.4 传送派发结论修正**：14.368 testsystem 世界多复现实测——`token.update({x, y})` 裸传送**会**派发 `tokenEnter/Exit/MoveWithin`（`animate:false` 与 `noHook:true` 变体均然，movement 数据携带、region.tokens 同步更新；服务端 preUpdate 对任何含移动字段的 update 都重算 `_regions` 并捕获 `_priorRegions`）。M0"裸传送不派发"的记录与本轮矛盾，原因未明（推测当时复现路径不同），**以本轮为准**。**`updateSource` 的边界（2026-09-26 二次复测确认）**：其为纯内存数据模型更新——不产生数据库更新操作、**不触发 updateToken 钩子**、不派发区域事件、`region.tokens`/`_regions` 包含性跟踪也不更新，故它**不在** updateToken 对账的可达范围内；其漂移仅是单端内存态与数据库的暂时背离，不改变服务端权威状态（账本、已施加 AE、区域成员的数据库记录均不变），由后续真实同步自然收敛，不设专项承接；决策 18 的 `updateToken` 去抖对账保留为防御未知无事件路径与未来核心行为变化的保险层，AURA-05 的定位由"唯一补正手段"调整为"兜底校验"。

## 7. 专项开发计划（2026-09-26 定稿）

本节是 M3 光环/区域施工与验收的唯一依据；状态约定、验证要求与提交规范沿用 [`V14_PLAN.md`](V14_PLAN.md) "使用方式与状态约定"。工作项编号 AURA-xx，与总计划 S3.x 的映射记录在总计划 M3 节。

### 7.1 目标与边界

**目标**：以 V14 Region 体系重建光环/AOE 能力并落地自动化。94 处标记条目的完成口径分三层统计（决策 21）：**全自动**——进入/回合结算开关 66 + 监听 AE 7 + 脚本编排 10 + 脚本分发 2 = 85 条（逐条评估，超出框架能力的降级）；**部分自动化**——仅标记 6 条（范围可视化、显示名与说明卡自动，规则面手动）；**手动**——借机窗口 2 条；待澄清 1 条（万毒心经，随 AURA-11 销项）。第 4 节清单在 AURA-12 逐条标注"自动结算哪些效果、仅显示哪些范围、仍需手动哪些规则"。

**统一原则**（实现与数据录入的共同准绳）：

1. **区域行为只回答"谁在范围内、何时进出、何时回合结算"**；效果逻辑尽量落在 payload AE（`system.changes` + 既有触发器脚本）。复杂动态规则（随机表、检定、按计数结算）以监听模式录在 payload AE 脚本上——**本轮零新增脚本引擎触发器**（第 6 节决策 8）。
2. **目标侧结算归行为**（enter/exit 对称结算 + 区域内回合事件）；**源侧生命周期归管理器**（时限递减、维持消耗、脱战清理、孤儿校验）。所有结算统一落在活动 GM 端（第 6 节决策 5），避免双端竞态。
3. **生效范围 = 可见范围 = 1-2-2-2 offsets 格子集合**（grid 形状）；进出判定直接采用核心包含语义（1×1 即中心点，多格为足迹，见第 3.2 节）；`circle`/`emanation` 形状不用于生效判定。
4. **数据侧触发层**：物品/招式脚本在既有时机（combatStart/hit/damaged/attack 等，先例充分）调用光环管理器 API 创建/撤销；生命周期清理分三层——数据脚本时机（正常流）、系统级 Foundry 钩子兜底销毁（决策 16）、combatEnd 与 ready（战斗期清空与孤儿校验）。光环参数显式写在脚本调用里，不在物品 system 上新增光环 schema（未来若要声明式配置另立工作项）。战斗中换装不自动创建光环（决策 25 边界）。

**边界（本轮不做）**：

- 不自建 Region 配置页（行为字段经 schema 由核心渲染，第 3.3 节）；
- 战斗外不做回合结算（核心回合事件本就只在战斗内派发；进出挂摘照常，第 6 节决策 6）；
- G2 视线/目标选取、G3 强制位移、G4 天候、G6/G7/G8 长尾维持手动（仅标记形态覆盖可视化）；G5 三项全部零引擎改动（怒气封锁现成链路、濒死免暴击现有受击脚本、黑烟阈值脚本，决策 33，见第 5 节 G5 补充），属性倒转/招式替换/免压制延后维持手动（决策 19）；
- 借机窗口 2 条维持手动（第 6 节决策 8）；
- 粒子为可选增强，默认关，性能不达标即取消（第 6 节决策 7）；
- 已导入世界的内容更新沿用既有重导入流程，不做专项世界迁移（区别于 AE 的格式迁移 AE-14）；
- `modifyMovementCost` 地形成本叠加（G1）实测通过后作为仅标记光环的可选后门，不进本轮承诺（第 6 节决策 9）。

### 7.2 工作项与依赖

#### A. 机制验证

- [x] AURA-01（承接 S3.3 悬案与第 6 节 13 条待实测）机制 spike：注册 `xjzlAura` 最小骨架（空 schema + 事件日志，`module/region/xjzl-aura-behavior.mjs`），在测试世界逐项实测并记录结论（构建号）：①行为注册与核心 RegionConfig/RegionBehaviorConfig 对行为字段的原生渲染（含本地化键挂载方式）；②grid 形状经 API 创建与进出事件，含 1×1 中心判定的运行时复验（源码已核实 1×1 仅测中心点，见第 3.2 节）；③`attachment.token` 跟随 grid 形状的整格平移；**源 Token 删除时核心自动删除附着区域**（源码 `TokenDocument._onDeleteOperation` 已核实，运行时复验）及区域内 token 的 exit 补发；④行为创建/启用时对已在区域内的 token 补发 enter（constants 契约注释为据，放置型光环的生效前提）；⑤`tokenTurnStart/End`、`tokenRoundStart/End` 到行为 handler 的派发端、相对 `ActiveEffect.registry.refresh` 的时序，以及与我方 EncounterManager 回合队列的实际交错（fire-and-forget 已从源码确认，实测交错形态作为幂等设计依据，决策 17）；⑥a 玩家移动的派发端与 isSelf 门控；⑥b socketlib 委托 GM 全链路与竞态归 AURA-03 验收；⑦性能三项：万剑归宗 20 米全格 offsets（921 格）渲染/判定、10+ 光环并存帧率、`modifyMovementCost` × 1-2-2-2 `measurePath` 劫持叠加；⑧矩形形状与按源朝向 90° 吸附旋转 offsets 的正确性（剑气白云 3×1、刀气旋风 3×3）。第 6 节 13 条的 ③④⑤⑥⑧⑨⑩ 在此销项；①传送对账归 AURA-05、②合成 Actor 归 AURA-03/05 验收、⑦濒死脚本归 AURA-10；另验证 `tokenMoveWithin` 事件携带的 movement 数据形态与触发粒度（决策 23"每移动结算一次"的依据），以及 Region 显示名在玩家视角的画布可见性（不可见则补自绘标签任务，复用旧模板标签绘制要点）。spike 不写业务代码，产出机制结论与实现取舍。→ **已完成（2026-09-26，14.368 实测，testsystem 世界，GM Gamemaster + `loginAs` 玩家身份）。完成范围界定：①～⑤、⑦、⑧、⑥ 的派发端与 isSelf 门控（⑥a）、movement 形态、显示名；⑥ 的 socketlib 委托全链路（⑥b）不在本项完成范围，已转 AURA-03 明确验收项**。逐项结论：
  - **① 注册三件套缺一不可**：system.json `documentTypes.RegionBehavior.xjzlAura`（服务端 game.model 于**世界启动时**构建，缺失则嵌入行为服务端校验直接失败，仅改客户端 CONFIG 无效）+ `CONFIG.RegionBehavior.dataModels.xjzlAura`（init 钩子注册，早于 i18n 的 schema 本地化扫描）+ zh-cn `TYPES.RegionBehavior.xjzlAura`（Localization.initialize 对 documentTypes 自动挂 typeLabels/typeHints 键，实测配置页标题"侠界光环: <名>"、身份/状态分区原生渲染，空 schema 无业务字段无报错）；`typeIcons` 无自动兜底需手动补键。
  - **② grid 形状与判定**：`shapes:[{type:"grid", offsets}]` 经 API 创建成功；**offsets 为场景绝对格坐标，核心 `getOffset` 返回 `{i: floor(y/size) 行, j: floor(x/size) 列}`——与生成器的 (i=列, j=行) 相反，AURA-04 写入时须轴映射**。1×1 token 中心点判定运行时复验通过；8 项边界矩阵（直邻/斜邻/二斜/混合步的界内外）全部与 AURA-02 生成器口径一致。
  - **③ 跟随与源删除**：`attachment.token` 必须传 token **id**（传 UUID 静默失效、不跟随）；grid 形状随源移动**整格平移 offsets**（GridShapeData.move 按前后 origin 的格差平移，`origin` 字段可省略、默认取 offsets[0] 格心）；**源 Token 删除 → 核心自动删除附着区域**，删除前对其内每个 token（含源自身）补发 `tokenExit`（movement:null）+ `behaviorDeactivated`（S3.3 悬案关闭）。
  - **④ 补发 enter**：行为创建（region 内已站 token）时对 `region.tokens` 逐个补发 `tokenEnter`（movement:null，user=行为创建者）；region 创建本身不补发（内联 behaviors 创建同样触发补发）；源码核对 disabled 翻转（启用）走同路径。
  - **⑤ 回合事件**：turn/round 四事件均由**活动 GM 端**工作流派发；时序恒为 `registry.refresh(阶段)` 先于对应区域事件；**战斗开始（round 0→1）会先派发一轮 tokenRoundEnd**（行为实现须 gate round≥1）；round 事件对区域内每个战斗员各派发一次、turn 事件仅当前行动者；我方 `updateCombat` 钩子（EncounterManager）**先于**核心区域事件执行 → 交错形态实测，幂等设计（决策 17）确认必要。
  - **⑥a 派发端与门控（本项已验）**：事件由触发者客户端本地 `_handleEvent` 执行、同时 socket 广播 `regionEvent`（各端收后同样执行，需 handler 内 `event.user.isSelf` 门控单端——applyActiveEffect 同款）；**玩家移动实测：handler 在玩家（发起者）客户端执行**（isSelf=true、isGM=false、user=Player2），决策 5 委托链路的前半段（发起者端触发）实锤。**⑥b socketlib 委托 GM 全链路与竞态未在本项验证**（spike 不写业务代码），转 AURA-03 明确验收项，竞态由决策 28/34 串行队列设计承接。
  - **⑦ 性能三项（921 格）**：20 米 offsets 实为 921 格（1-2-2-2 度量）；创建 ~14ms、`testPoint` 1000 次 0.3ms（Set 查表 ~0.3µs）、`testInsideRegion` 100 次 0.3ms、渲染帧率不受损（vsync 封顶）；12 个 r=5 光环并存批量创建 36~42ms、帧率中位 80（创建瞬时最低 53）→ **AURA-07 粒子可行，阈值可放宽**；`modifyMovementCost` × 1-2-2-2 **叠加成立**：`movement.passed.cost` 承载"1-2-2-2 × 地形倍率"终值（6 直格穿 3 格 walk=2 → cost 9；3 连斜 → cost 5=1+2+2），G1"地形成本"后门实测可行。
  - **⑧ 矩形与旋转**：3×1 生成器输出经 `rotateOffsets90` 顺旋 1 次后建区，覆盖格逐格验证一致（中心+正南两格在内、东西北不在）；`snapDirectionToQuarterTurns` 吸附数值正确。
  - **movement 数据形态与粒度（决策 23 依据）**：movement 携带 `{id, subpathId, chain[], origin, destination, passed:{waypoints[{x,y,elevation,action,cost,…}], distance, cost, spaces, diagonals}, pending, history, split, method, …}`；**触发粒度=每次移动操作（一次拖动 N 格 = 1 次 moveWithin，与输入方式无关）**；**行为订阅 TOKEN_MOVE_* 会令核心把移动路径按区域边界切分 checkpoint**（一次跨多区域的移动拆成链式子移动）；**进入移动同时触发 tokenEnter+tokenMoveWithin、移出移动触发 tokenMoveWithin+tokenExit、同格重放也发 moveWithin** → AURA-03 的"每移动结算一次"须按 movement.id 去重并在 enter 分流，防踏入双结算。
  - **S0.4 结论修正（决策 39）**：裸 update x/y 传送在 14.368 **会**派发 enter/exit/moveWithin（animate:false 与 noHook:true 均然），与 M0 记录矛盾，以本轮多复现为准。
  - **显示名**：核心不在画布渲染区域名（placeable 无文本子节点、无 label API，GM/玩家一致）→ 需自绘标签，归 AURA-06/快建工具（复用旧模板标签绘制要点）。
  - 临时数据（9 token、7 region、1 combat、运行时探针）已全部清理。

#### B. 核心框架

- [x] AURA-02（S3.5 前半）1-2-2-2 offsets 形状生成器：从 `module/measured-template.mjs` 提取圆形覆盖格遍历算法至 `module/utils/`，扩展为形状生成器——圆形（中心格 + 半径格数 → offsets，主体形态，约 99% 条目）、矩形（W×H 格）、创建时按源 token 朝向的 90° 吸附旋转变换（决策与实现见第 3.2 节）；Node 断言覆盖若干半径的期望格子集合（含对角 1-2-2-2 口径样例：直邻、斜邻、二斜等距）与矩形/旋转用例；measured-template.mjs 本项暂保留（AURA-12 删除）。无依赖，可先行。→ **已完成（2026-09-26；2026-09-26 审阅修订：收紧为整数半径契约）**：`module/utils/aura-shapes.mjs`（纯函数、零 Foundry 依赖，Node 可测）——`gridStepDistance`（单段中心到中心 1-2-2-2 计费，与 measurePath 劫持一致：直行 1、首斜 1、后续斜 2）、`generateCircleOffsets`、`generateRectangleOffsets`（W×H + 锚格）、`rotateOffsets90`（90° 吸附，整数格天然吸附、-0 归一化）、`snapDirectionToQuarterTurns`；输出统一行优先排序去重、序列化稳定。**与旧模板的等价范围：整数半径 0～20 逐半径复算完全一致；小数半径存在覆盖口径差异——r=0.9 时旧欧氏剪枝（r+0.5 格容差）排除 (1,1) 型斜邻格（欧氏 ≈1.414 > 1.4）而 1-2-2-2 计费（≤ r+0.1）包含它（审阅复算确认）——故按决策 20"米即格、按格数存储"仅接受整数半径、小数入参显式拒绝**。`tests/aura-shapes.test.mjs` Node 直跑 11 组断言全部通过：r=0/1/2/3 精确格子集合（9/21/37 格及角格排除）、直邻=斜邻=1、二斜(2,2)=三直(3,0)=3 等距、整数半径契约拒绝用例（0.9/1.5/2.5/2.95/-1/NaN/Infinity）、r=20=921 格包围盒独立重算一致、矩形锚格与越界校验、旋转四向/归一化/锚格不动/整数吸附、朝向吸附边界。运行时接入注意（AURA-04）：核心 GridShapeData offsets 的 i=行(y)、j=列(x)，与生成器 i=列、j=行 相反，写入时须轴映射；半径按格存储（决策 20），生成器不读场景 grid 设置。
- [ ] AURA-03（S3.5 后半）`xjzlAura` 行为类型完整实现：`CONFIG.RegionBehavior.dataModels` 注册 + 本地化；schema 字段：结算模式（enter/round/mark 三选一或组合）、半径（非负整数格）、形状（圆/矩形 + 朝向）、阵营过滤（友方/敌方/全体）、是否含自身、payload 引用（源物品 UUID + 效果名 + 档位）、每回合限一次节流、直接结算动作（伤害/治疗；数值支持固定值或按源角色 rollData 解析的公式，如 `0.4*@neixi`）、显示名、颜色、粒子样式（AURA-07 才启用）。enter/exit 对称结算：进出判定直接采用核心包含语义（第 3.2 节，不做二次判定、不自研状态机）；enter 正常走门面 `addEffect`（叠层/刷新/满层语义照旧）并记录贡献账本（决策 22：叠加前后层数差存 region flags，非叠层记"是否本光环创建"）；**enter 以账本存在性幂等**——同一 Region＋Token 已有账本条目（`active: true`）时不再施加、直接视为已生效；每次成功处理的 enter 均记录条目（含贡献 0 与 created=false 的情况，`active` 是唯一幂等与对账依据，`contributedStacks`/`created` 只决定 exit 摘除方式，exit 后清除账目，防重复 enter 触发门面重复刷新/覆盖，决策 37）；exit 按账本摘除——可叠层按贡献数 `removeEffect`，非叠层被其他同 payload 光环覆盖时账本转移、否则删除，清空型条目按 schema 选项全清；直接伤害/治疗走 `applyDamage/applyHealing` 事务；区域内格间移动经 `tokenMoveWithin` 按**每次移动**结算一次（决策 23，万剑归宗用）；非 GM 客户端收到事件一律经 socketlib 委托活动 GM，本端即 GM 时直接执行，**账本操作按目标 Actor ＋ payload 串行队列执行、在执行回合核对覆盖状态**（入队前重查仅作快速过滤；写入完成后二次核对兜底 region 删除等场景；串行化解决"进→出→再进"交错与并发贡献记重，决策 28/34）；回合事件对区域内单位结算（固定数值动作；复杂规则按统一原则落 payload AE 脚本）；mark 模式零结算；节流不用计数器重置，以 {战斗 ID, 轮次} 为键（区域外整轮的 token 收不到该区域的 roundStart，决策依据第二轮审阅）；所有结算幂等——同一单位同一事件不重复结算（决策 17）。**实机验收项（承接 AURA-01 ⑥b）：socketlib 委托 GM 全链路——玩家移动触发非本端 handler、经 executeAsGM 落活动 GM 端单端结算（无往返当本端即 GM），链路与竞态随本项实机验收（spike 已实测玩家端 handler 在发起者客户端执行）。** → **代码完成待实机（2026-09-26，实机验证留下次会话）**。实现落位：
  - `module/region/xjzl-aura-behavior.mjs`（spike 文件已收编为正式实现，`game.xjzl.auraSpike` 与 `[aura-spike]` 日志已移除）：schema 全字段（enter/round 开关＋范围生成参数＋faction/includeSelf＋payload 引用＋enterAction/roundAction {kind, amount, type, pierce}＋moveWithin＋throttlePerRound＋roundTiming＋exitClear）；字段 label/hint 走 `XJZL.AuraBehavior.*` 本地化键。
  - `module/region/xjzl-aura-ledger.mjs`（新增）：GM 端串行队列（键 `actorUuid|payloadKey`，链接 Actor 跨 Token 一致、合成 Actor 的 uuid 即 Token uuid——决策 34"目标 Actor＋payload"口径；payloadKey 由 handler 入队时携带，region 删除补发的 exit 拿不到行为配置，串行键必须同步确定）；enter 执行序＝幂等（账本存在性，决策 37）→ 阵营过滤 → 节流（命中仍记 active 条目保持对账一致）→ 门面施加＋记贡献 `{active, stacks, created, stackable, slug, exitClear}`（条目自足：slug 为实际显式 slug、exitClear 为施加时配置，摘除不依赖行为当前配置）→ 直接动作 → 写入后二次核对补偿（决策 28，含 exitClear 口径）；exit 按本 region 账本该 token 的全部有效条目摘除＋覆盖转移（按实际 slug，覆盖判定遍历 Actor 全部 Token）＋清账目，region 已删用入队快照（决策 22）；moveWithin 按 movement.id 去重且只对 origin/destination 均在 grid offsets 内的纯区域内移动结算（spike：进入移动同时触发 enter＋moveWithin，按几何分流防踏入双结算）；round 结算 gate `round>=1`（spike：战斗开始 round 0→1 先发一轮 tokenRoundEnd）＋timing 匹配（choices 值即 CONST.REGION_EVENTS 事件名）＋去重集合幂等（决策 17）。
  - 已知实现取舍：节流/账本 flags 写入拆为 region.update 单键（未合并批量）；对账对"覆盖内但被过滤"的 token 每轮空转一次 enter（filter 后即返，零副作用）；公式解析失败警告并按 0 处理（不结算）。
  - **静态审阅修复（2026-09-26，第一轮代码审阅，修复后才进入实机验收）**：①**P0 显式 slug payload 不可摘除**——账本键保持配置级（效果名 slugify，作分组与定位），账本条目新增 `slug` 字段存 resolvePayload 解析出的**实际** slug（显式 flag 优先，与门面 getSlug 口径一致）；enter 的 before/after 查找、exit/补偿/清空型的摘除、对账快照补摘一律按条目 `slug` 执行——此前按配置级键查找永远落空，"浩渺气海"（显式 slug `haomiaoqihai`）类 payload 会被误记 created=false 而退出不摘。②**P1 direct 键点路径污染**——无 payload 光环的账本键由 `direct:.`（展开成嵌套垃圾键）改为 `direct:${behaviorId}`（Foundry id 无点），payloadKeyOf 增补行为文档 id 参数并对键显式剔除点。③**P1 对账绕过 mark 模式**——执行端 enter 补 `system.enterEnabled` gate，与事件入口同口径。④**P1 跨光环移动结算被抢**——moveWithin 去重键由全局 movement.id 改为 `regionUuid|tokenUuid|movement.id`，一次移动穿过多个光环时各自独立结算。
  - **静态审阅修复（2026-09-26，第二轮复审）**：⑤**P1 exit 条目自足**——`exitClear` 随 enter 记入账本条目，exit 执行不再依赖行为当前配置；摘除范围改为本 region 账本中该 token 的**全部**有效条目（正常单配置单键；配置页更换 payload 引用后旧键条目随同一次退出清理），region 删除时用入队快照（snapshots 数组）——修复删除补发 exit 时行为不可达导致"退出全清"失效、与改 payload 引用后旧特效残留两个问题；覆盖转移改按实际 slug 匹配其他光环账本条目。⑥**P1 队列按 Actor 串行**——队列键由 `tokenUuid` 改为 `actorUuid`（链接 Actor 跨 Token 一致、合成 Actor 的 uuid 即 Token uuid），对账提交同步携带，决策 34 的"目标 Actor＋payload"口径落实。
  - **静态审阅修复（2026-09-26，第三轮复审）**：⑩**P1 多 Token 共享 Actor 误删**——覆盖转移的判定遍历 Actor **全部** Token（链接 Actor 的 A、B 两 Token 同在光环内时，A 退出不得删除 B 仍需的共享 AE；同光环内另一 Token 的覆盖同样有效），转移目标写覆盖 Token 在存活光环账本中的条目；触发频率为"非叠层 created 退出"分支，遍历成本可忽略。⑪**P2 在途补偿遵守 exitClear**——二次核对的补偿清理与正常退出同口径（清空型整条清除）。⑫**payload 不可解析孤儿校验**——ready 校验补第三项：源物品在但效果名匹配不到时警告降级（保留区域与直接动作），与计划"清理**或**降级仅标记并警告"对齐（源物品整体失效仍清理）。
  - **静态审阅修复（2026-09-26，第四轮复核）**：⑬**P1 转移写错账本**——多 Token 覆盖判定扩展后，创建标记仍写到退出 Token 的键下（成为永久脏条目，覆盖 Token 退出时 created=false 不摘、共享 AE 残留）；修正为 `findOtherCoveringAura` 返回覆盖 Token 的 tokenId 并写入其条目。
  - **静态审阅修复（2026-09-26，第五轮复核）**：⑯**P1 覆盖查找不依赖当前画布**——核心 `getActiveTokens` 只返回当前画布场景且已渲染的 Token，GM 查看别的场景时覆盖判定会漏掉同 Actor 仍在光环内的 Token 而误删共享 AE；改为在光环所在场景内按 Actor uuid 过滤 `scene.tokens`（region 只属于一个场景，既正确又不依赖画布，成本更低），管理器 `#countEnemies` 的源 Token 判定同步修正。⑰**auto 朝向与手动固定值共存**——行为 `_onUpdate` 在 quarterTurns 被手动修改时同步参数快照（refreshAura 依赖快照区分"auto 跟随源朝向"与"手动固定"），手动固定值不再被 refreshAura 的 auto 恢复覆盖；未手动改过时保持 auto 语义按当前朝向吸附。
  - **静态审阅修复（2026-09-26，第六轮复核）**：⑱**P1 朝向快照同步引用错误**——`_onUpdate` 中裸 `region` 引用未定义，手动改 quarterTurns 时抛错并中断形状重算；改为 `this.region`（fire-and-forget + catch，与 shapes 重算互不阻塞）。⑲**P1 覆盖查找跨场景**——上一轮只查光环所在场景，同一关联 Actor 的另一 Token 在**其他场景**被同 payload 光环覆盖时退出仍会误删共享 AE；改用核心 `getDependentTokens({concreteOnly:true})` 跨场景取依赖注册表的实际 Token 文档（合成 Actor 只返回自身，注册表查找无全局遍历成本）。
  - **接受的边界（2026-09-26 用户确认）**：①配置页更换 payload 引用**不即时切换**已覆盖目标——旧效果保留至退出/重进（exit 按旧账本条目清理），需要即时切换用 `refreshAura` 重建（exit→enter 天然切换）；档位联动的主路径是数据脚本调 refreshAura，配置页细调是低频 GM 操作，不为其引入账本迁移与即时重挂的复杂度（行为 `_onUpdate` 注释已写明）。②socket 层不做快照可信校验——光环结算 socket 入口的信任模型与系统既有 `applyDamage`/`addEffect` 等 GM 委托入口一致，不单独加固。
  - 实机验收清单（下次会话）：socketlib 委托全链路（玩家移动→GM 端结算、多 GM 不重复）；账本闸门用例的代码路径就位（AURA-08 专用测试物品）；非链接 Token 合成 Actor 施加/清理（13-②）。
- [ ] AURA-04 光环管理器：公开 API 挂 `game.xjzl`（与 `api.effects` 并列，最终命名实施时定稿）：`create(source, params)`（AURA-02 生成器算 offsets → 建 region：grid 形状 + `visibility: ALWAYS` + 颜色默认按元素/品质（可在核心配置页改）+ 显示名 + 跟随模式加 `attachment.token` + 挂 xjzlAura 行为；源支持角色 Token 与指定格点）、`dismiss(label|regionId)`、`query(label)`、`refreshAura(label)`（档位/半径变化后重建）、`queryTokens(source, {radius 或 offsets, 阵营过滤等})`（**无实例范围查询**：按 AURA-02 生成器现算 offsets、对候选 token 按包含口径判定（1×1 中心、多格足迹任一点，决策 35）返回 Actor 列表，不创建文档——供五雷等 attack 触发脚本使用，决策 30；实现须显式传入文档**源**位置与尺寸调用 getContainmentTestPoints，无参调用取 prepared 值可能处于动画中，而 Region 判定用 source 值）；单实例标签替换（删旧建新，核心对旧 region 补发 exit 完成清理）。**来源生命周期（销毁时机按效果原文，决策 16）**：架招光环由既有 `stopStance` 路径（关架招/被破/濒死共用）直调 `dismiss`；装备光环卸下即毁、运功光环切换即毁——经系统级 Foundry 钩子 `updateItem`/`deleteItem`/`updateActor` 监听源状态（钩子内查源物品映射，无命中零开销返回；不新增脚本触发器）；时限在战斗回合边界统一递减（GM 端单一入口，与 AE registry 无耦合）；维持消耗在源角色回合末结算且**单一归属管理器**（区域行为不扣钱，决策 17）；combatEnd 清空战斗期光环；ready 孤儿校验与恢复（region flags 持久化源物品 uuid、到期轮次、维持状态、战斗 ID 与**施加账本**（决策 22）；战斗已结束、源物品失效、payload 不可解析的 → 清理或降级仅标记并警告）。 → **代码完成待实机（2026-09-26，实机验证留下次会话）**。实现落位：
  - `module/region/xjzl-aura-manager.mjs`（新增，挂 `game.xjzl.aura`）：`create/dismiss/query/queryLabels/refreshAura/queryTokens/reconcile/reconcileCombat/dismissBySource/validateOrphans`；生成器 offsets 经轴映射（生成器 i=列/j=行 → 核心 i=行/j=列）＋锚格叠加后写入 `shapes:[{type:"grid", offsets}]`；`attachment.token` 传 token **id**（UUID 静默失效，spike 结论）；`quarterTurns:"auto"` 按源 Token `rotation` 吸附 90°；单实例先删后建。
  - 生命周期三层：①脚本时机直接调 API；②钩子兜底（init 时注册）——updateItem `system.equipped:false`→equip 光环销毁、deleteItem→按源物品销毁、updateActor `system.martial.active_neigong` 变化→yungong 光环销毁、架招经 `stopStance`（actor.mjs 新增 `dismissBySource(stance)` 直调，濒死共用路径覆盖）；③时限每轮开始递减（`currentRound - startedRound >= rounds` 即散）、维持消耗在源角色 turnEnd（updateCombat 钩子内、按 {combatId, round, combatantId} 去重，实际扣取不足即散，perTarget 按覆盖敌人数）、round 回 0 与 deleteCombat 清 combat 光环、ready 孤儿校验（战斗已结束/源物品失效→清理＋警告）。
  - region flags `xjzl-system.aura` 持久化 label/lifecycle/源 uuid 三元组/combatId/duration/maintain/params 快照（refreshAura 重建依据）；levels 跟随源 Token 所在层、放置型空数组（单层场景语义"所有层"，多层场景由 levelIds 显式指定——**多层场景行为列入实机验收**）。
  - 已知实现取舍：阵营判定以源 Token disposition 为基准（同侧友方、对置且非中立敌方，与战局 HUD 三色口径一致，实机可调）；放置型光环阵营判定回退源 Actor 的活动 Token（resolveSourceToken）；`queryTokens` 忽略高度层判定（地面光环口径）。
  - **静态审阅修复（2026-09-26）**：⑤**P1 时限锚定**——`durationRounds` 存在即记录创建时战斗 id（此前仅 `lifecycle:"combat"` 记录，普通生命周期光环永不到期）；⑥**P1 跨战斗误清**——`#clearCombatAuras` 只按 combatId 匹配（此前 `lifecycle:"combat"` 无条件删除，多场景并行战斗时误清他战斗光环），孤儿校验补"combat 光环无 combatId"清理；⑦**P2 配置页改范围不生效**——行为新增 `_onUpdate` 钩子：范围字段变化时以当前形状原点为锚重算 offsets 并写回 region.shapes（核心随之补发进出、账本收敛），`refreshAura` 改为从行为 system 读形状参数（快照仅兜底管理器字段）；**P2 半径契约**——create/queryTokens 对小数半径显式拒绝（此前 `Math.trunc` 静默截断，违反 AURA-02 契约），轴映射提取为 `toCoreOffsets` 纯函数（aura-shapes.mjs，含 Node 断言）供管理器与行为热更新共用。
  - **静态审阅修复（2026-09-26，第二轮复审）**：⑧**P1 改半径移动中心**——创建数据显式写 `origin`（锚格格心像素；核心 GridShapeData 兜底取 offsets[0] 格心即西北角，热更新会以它反推锚格造成偏移），热更新写回时原样保留 origin，缺失时警告跳过重算不猜锚；跟随平移由核心同步更新 origin。⑨**P1 维持消耗语义**——`maintain.perTarget` 修正为"每名覆盖敌人的追加消耗"（cost = amount + perTarget×敌人数，表达"按人数×20"），此前按布尔只加人数；仅设 maintain 的光环同样绑定创建时战斗 id（此前不绑则永不扣费）。
  - **静态审阅修复（2026-09-26，第三轮复审）**：⑫ready 孤儿校验补"payload 不可解析"项：源物品在但效果名匹配不到时警告降级（保留区域与直接动作），与计划"清理**或**降级仅标记并警告"对齐（源物品整体失效仍清理）。
  - **静态审阅修复（2026-09-26，第四轮复核）**：⑭**refreshAura 读行为当前配置**——重建参数改为整体合并行为 system 的实时值（payload/动作/过滤/形状全部随配置页改动生效，此前只读形状字段、payload 仍取创建快照，"配置页改 payload 后调 refreshAura 即时切换"的口径才能成立）；`quarterTurns:"auto"` 重建时恢复源语义按当前朝向重新吸附。⑮**孤儿校验改读行为实时 payload**——配置页改过 payload 后快照过时会误报/漏报，校验与运行时一致读 behavior.system。
  - **静态审阅修复（2026-09-26，第五轮复核）**：⑰**auto 朝向与手动固定值共存**——行为 `_onUpdate` 在 quarterTurns 被手动修改时同步参数快照（refreshAura 依赖快照区分"auto 跟随源朝向"与"手动固定"），手动固定值不再被 refreshAura 的 auto 恢复覆盖；未手动改过时保持 auto 语义按当前朝向吸附。
  - 实机验收清单：跟随/放置全周期建删、整格平移、时限/维持/脱战清理、refreshAura 档位重建、queryTokens 多格足迹、卸下装备/切换运功/解除架招的自动销毁、无网格场景禁用提示、配置页改半径后范围原地生效且不移心。
- [ ] AURA-05 传送对账兜底（**必选项**，定位按决策 39 修正）：**14.368 实测 `document.update` 裸传送已派发 enter/exit/moveWithin（决策 39）**——常规传送依赖事件即时补正；本项定位为**兜底校验**：`updateToken` 钩子去抖对账保留（防御未知无事件路径与未来核心行为变化）+ 回合事件对账（战斗回合边界补扫）；比对覆盖状态与施加账本（决策 22），漏挂补挂、漏摘按贡献数补摘，均走门面，对事件已即时补正的常态零操作。**验收方式（2026-09-26 审阅修订）：不以 `updateSource` 作为触发用例**——其为纯内存数据模型更新，不产生数据库更新操作、不触发 updateToken 钩子与区域事件、包含性跟踪也不更新（AURA-01 二次复测），对账钩子对其不可达；改用**强制构造失配**验收（测试钩子临时抑制事件派发，或直接注入"覆盖状态 ↔ 账本"失配后调用对账入口），验证补正逻辑本身；`updateSource` 的漂移仅是单端内存态与数据库的暂时背离，不改变服务端权威状态（账本、已施加 AE、区域成员的数据库记录均不变），由后续真实同步（下一次真实移动/文档刷新/重连）自然收敛，不设专项承接。非链接 Token 的合成 Actor 分支在此与 AURA-03 一并实测（第 6 节 13-②）。 → **代码完成待实机（2026-09-26，实机验证留下次会话）**。实现落位：
  - 对账入口：`AuraLedger.reconcileToken(tokenDoc)`——以核心包含性跟踪 `token._source._regions` 为覆盖基准，比对有效账本条目（`active: true`），覆盖内无账目→补挂（复用 enter 队列，节流与过滤照常生效）、覆盖外有账目→逐条补摘（复用 exit 队列）；hidden region 与 disabled 行为跳过（事件停摆时账本冻结，防止误补）。
  - 接线：updateToken 钩子（x/y 变化）按 token 去抖 500ms 对账（`AuraManager.init` 注册，仅活动 GM）；回合事件对账在 updateCombat 轮边界（round>=1）对全体战斗员补扫（`reconcileCombat`）。
  - 公开验收入口：`game.xjzl.aura.reconcile(target)`（TokenDocument 或 Scene）——实机验收时注入"覆盖↔账本"失配后调用，验证补正；常态下事件已即时补正、对账零操作。
  - 实机验收清单：强制失配后一拍内补正（updateToken 去抖生效、战斗内外均可）；失配注入方式（临时钩子抑制事件派发或直接改 flags 账本）；合成 Actor 分支；对已补正常态零操作确认。

#### C. UI 与工具

- [ ] AURA-06（S3.2/S3.4/S4.3）快建工具：`getSceneControlButtons` 注入按钮（注入机制 S0.3 已验证）；轻量弹窗（非负整数半径/持续回合/颜色/跟随或固定/显示名）；画布放置格子吸附，光环中心强制吸附格心（跟随模式改为选源 Token）；默认创建 mark 模式光环，需要结算的经核心配置页在行为里后补；`data/macros/utility.json` 旧"创建跟随光环"宏改写为指向工具的说明宏（**不删除**，口径与 S4.3 一致），宏数据变更后 `game.xjzl.seed.macros()` 重建。
- [ ] AURA-07（可选，可砍）粒子增强：行为 schema 粒子样式字段 + V14 原生 `ParticleGenerator`（effect 模式、固定数量）+ 系统设置总开关（默认关）；AURA-01 性能结论不达标即取消本项，不影响出口。

#### D. 数据落地（依赖框架齐备；每批均含 automationNote 同步更新、改动 JSON 过 `JSON.parse`、对应 `game.xjzl.seed.<type>()` 重建与导入验证）

- [ ] AURA-08 样板批次（5 条 + 专用测试物品，铺量前的闸门）：无涯（跟随·友方·分档·单实例·时限）、浩渺气海（跟随·敌方·非叠层——第四轮核实"湘水"与"浩渺气海"均 stackable:false，二者同作非叠层敌方样板）、寒霜（放置·全体·单实例·1 回合）、灼日（跟随·敌方·回合伤害·架招终止）、青海（跟随·全体含己·维持消耗·三档）。**贡献账本主测使用专用测试物品**：data/ 现有光环 payload 无一为可叠层（第七轮核实），闸门以临时物品的 effects 中的测试 AE（stackable:true + maxStacks:3）与双光环 Token 搭建、完成后清理（沿用 AE 测试临时数据惯例）——**置于临时物品 effects 中是为了走通"源物品 UUID＋效果名"的 payload 解析路径（AURA-03 契约）**，不与具体内容条目耦合；真实 payload 的叠层语义由门面既有行为保证。用例（决策 22/37/38，验收写具体层数）：进 A→1 层、进 B→2 层、退 A→1 层、退 B→整条移除；**预存 1 层→进入变 2 层→退出回 1 层**；**满层钳制独立用例：预存满 3 层，进退均为 3 层**；**重复 enter 两个特例均只处理一次（决策 37）：测试 AE 已满层（贡献 0）时重复 enter 不刷新时长，目标预存非叠层同 slug AE（created=false）时重复 enter 不覆盖数值**；清空型退出全清；**进→出→再进与双光环同时进入经串行队列不重复施加、不记重贡献（决策 34）**。逐条通过第 7.4 节矩阵对应行后才进入铺量。
- [ ] AURA-09 铺量批次：剩余进入/回合结算 61 条（分档联动、时限、维持消耗、计数上限等按第 3.1 节选项组合；**湘水**在本批作非叠层敌方样板；剑痕/箭雨的主体已在开关承载，仅补联动脚本）+ 脚本编排 10 条（attack/damaged 触发创建、`token.regions` 反查脚下光环、管理器实例 API 消耗：暗刻、遁光×2、踏莲、天神断海、碧火×3、热恼地狱、**五雷**——attack 触发改用 `queryTokens` 无实例查询（决策 30/35：包含口径与 Region 一致，多格目标按足迹任一点判定）替换 Math.hypot 手算距离，作为决策 26 的迁移样板）。**逐条评估自动化成本（决策 21）**：能用框架与现成机制表达的（含决策 33 的暴击处理：**新建公共"黑烟"AE**——现数据无 payload 仅有提示脚本，四招共用，含出招脚本负向修正 `critThresholdMod` 与回合开始毒伤的挂接点，automationNote 写明 forceCrit 与手动暴击例外（决策 36）；"生民立命"现有 preDefense 脚本即濒死免暴击实现，无需改动；"万世开太平"payload 已现成）全自动；超出框架能力的降级为提示卡片 + 手动，并在 automationNote 与第 4 节清单如实标注，不强行实现。
- [ ] AURA-10 监听 AE 批次（7 条）：不可见监听 AE（`showIcon: 0`、无 changes）+ 既有触发器脚本（death/dying/attack/damaged/turnStart）反向驱动持有者效果；离开范围（AE 被摘）后不再触发；随风潜入夜的"残疾检定"按濒死（dying）映射，口径为每次进入濒死计一次（第 6 节决策 8）；实测濒死/死亡时点脚本仍随 Actor 正常执行（第 6 节 13-⑦）。
- [ ] AURA-11 仅标记与分发批次：6 条仅标记（region + 显示名 + 创建时一次说明卡 + 回合提醒默认关）+ 进战脚本分发 2 条（赤霄剑、马头琴——后者现 combatStart 脚本已符合原文"所有友方"，仅归类调整）+ 万毒心经澄清销项（对照规则书：有光环面则按形态归入相应批次，无则改 automationNote 并在第 4 节清单销项）。

#### E. 收尾

- [ ] AURA-12（S3.1）弃用清理与文档同步：删除 S1.14 全部注释块（全局搜索 `【V14 升级 S1.14 停用，代码仅注释未删除】` 标记定位）；删除 `module/measured-template.mjs` 与 `module/applications/aoe-creator.mjs`（1-2-2-2 算法已由 AURA-02 提取）；文档：SCRIPT_ENGINE.md 增光环管理器 API 与光环脚本编写指引、SEEDING_GUIDELINES.md 增光环录入约定（标记键、payload 命名与分档、行为参数、automationNote 口径）、PROJECT_MAP.md 与 CLAUDE.md 同步入口与事实源；本文件第 4 节清单逐条标注实现归属并销项。

### 7.3 实施顺序与出口

1. AURA-02 无依赖可先行；AURA-01 spike 与之并行，机制结论出来后定 AURA-03/AURA-05 的实现取舍（尤其对账方案与性能预算）。
2. AURA-03 → AURA-04 → AURA-05 为框架主线；AURA-06 依赖 02/03/04；AURA-08 样板在框架与工具齐备后实施。
3. AURA-09/10/11 为内容批次，排在框架与样板闸门之后（决策 21：框架先行，内容自动化是最后一步）；批次间可穿插（监听批次只依赖行为 enter/exit，可最早开始）；AURA-07 粒子排最后。
4. AURA-12 在数据批次完成后收尾。
5. **框架出口**（对齐总纲 M3 出口标准）：AURA-01～06 与 08 完成——静态/跟随区域、1-2-2-2 判定、进出结算、生命周期清理在测试世界通过，旧 AOE 等价能力恢复。**内容出口**：AURA-09～11 完成，94 条全部销项（手动 2 条与待澄清结论记录在案）。S3.8（标尺与移动劫持重适配）不属本专项，按总计划另行实施。

### 7.4 验收矩阵与完成标准

| 场景 | 必须看到的结果 |
|---|---|
| 跟随光环全周期 | 装备/架招/进战触发创建（格心吸附；1-2-2-2 覆盖格与旧模板高亮口径一致）；源移动后区域整格平移，边界变化对 token 补发进/出；卸下/解除/被破/脱战/源 Token 删除后区域与已挂 AE 全部清理，无残留、无报错 |
| 判定口径 | 半径 N 米 = N 格，N 为非负整数（1-2-2-2，"米"即格、无换算）；1×1 token 中心点判定、多格 token 足迹判定（核心语义）；光环圆心吸附格心；无网格场景禁用创建并有提示 |
| 进出结算 | 友方/敌方/全体/不含自身过滤正确；放置时已在区域内的单位立即获得 enter 结算（行为激活补发）；payload 分档数值正确；直接伤害/治疗走事务（飘字/统计正常）；"每回合限一次"节流以 {战斗 ID, 轮次} 为键（区域外整轮不残留旧计数）；贡献账本正确（决策 22：双光环进退层数 1→2→1→移除、预存 1 层→进→2 层→退→回 1 层、满层钳制独立用例进退均满、清空型退出全清、重复 enter 只处理一次含满层贡献 0 与 created=false 特例（决策 37））；**进→出→再进与双光环同时进入经串行队列不重复施加、不记重贡献（决策 34）**；Region 删除时施加在途不残留（决策 28 二次核对补偿） |
| 暴击抑制 | 黑烟内角色自然暴击不可达（黑烟 AE 出招脚本负向修正阈值，决策 33；forceCrit 与手动暴击直传绕过为已知边界，决策 36）；"生民立命"范围内友方濒死受击不承受暴击伤害倍率（现有 preDefense 脚本），非濒死正常（反例验收）；战报仍显示暴击为已知边界 |
| 回合结算 | 区域内单位回合开始/结束结算正确且幂等（同一单位同一回合不重复扣血/扣维持）；维持消耗单一归属管理器、与区域行为不双扣；与核心 AE 到期清理（registry）先后无冲突、不重复；维持不足即散（青海语义不变） |
| 格内移动 | 万剑归宗等单 region 大区域：token 在区域内格间移动时经 `tokenMoveWithin` 每次移动结算一次（决策 23），不依赖重新 enter |
| 时限与单实例 | N 回合到期自动消失（战斗回合边界）；同名标签替换旧实例；计数上限条目（腐蛊雾霭 1+up 等）正确 |
| 传送对账 | `document.update` 裸传送进出光环经事件即时补正（14.368 实测有事件，决策 39）；对账兜底以**强制构造失配**（测试钩子抑制事件派发或注入"覆盖 ↔ 账本"失配）验证：updateToken 去抖对账在战斗内外均生效、一拍内补正施加状态（对已补正的常态零操作）；`updateSource` 纯内存漂移（无钩子无事件、不改变服务端权威状态）由后续真实同步自然收敛；合成 Actor（非链接 Token）施加/清理正确 |
| 权限与执行端 | 玩家移动触发的结算全部落在活动 GM 端；无权限用户不能绕过门面；多 GM 在线不重复结算 |
| 监听 AE | 死亡/濒死/出招/受伤反向触发持有者效果；离开范围（AE 被摘）后不再触发；随风潜入夜按濒死口径计数 |
| 脚本编排 | 暗刻立于其上出招引爆并消耗实例；碧火检定扑灭与火球消耗；万剑湮灭联动返还；破招/格挡触发放置；五雷每次出招经 `queryTokens` 给范围内敌人叠层（与 Region 同口径含多格足迹，决策 35，替换手搓距离） |
| 仅标记与快建工具 | 工具放置格心吸附、显示名画布可见；说明卡只发一次；mark 模式无任何自动结算副作用；旧宏改写后经 seed 重建生效 |
| 性能 | 万剑归宗规模 offsets 与 10+ 光环并存的帧率数据记录在案（粒子开关阈值依据）；`modifyMovementCost` 叠加结论记录（G1 后门依据） |
| 数据批次 | 每批 seed 重建后导入验证：脚本语法、payload 结构、automationNote 与实际自动化程度一致（降级条目如实标注"提示+手动"）；回归该批次涉及物品的既有时长/叠层行为不变化 |

每个改动的 `.mjs` 执行 `node --check`，改动的源 JSON 执行 `JSON.parse`；实机验证记录构建号、操作步骤、结果与未覆盖项，未实机验证的项保持 `[ ]`，不以源码推断代替验收。数据批次条目多，按文件分批实施与验证。

### 7.5 后续候选（本轮不做，2026-09-26 记录）

1. **手搓距离计算迁移**：data/ 中存在一批"出招时给范围内单位上效果"的脚本用 Math.hypot 手算距离、口径与 1-2-2-2 不一致（五雷为首个实例，AURA-09 改用管理器区域查询）；用户指示后续找出同类效果逐批迁移到区域查询，获得 1-2-2-2 口径与画布可视化（决策 26）。
2. **EQUIP 触发器**：战斗中换装自动创建光环的解除方案（决策 25 边界）——触发器派发约十行 + SCRIPT_TRIGGERS/TRIGGER_CHOICES/本地化/文档同步，但每件装备需成对维护 equip 与 combatStart 脚本；立项前先评估数据维护成本。
3. **G5 延后三项**：属性倒转（阴阳变）、招式替换（恒河结沙）、免压制（热恼地狱）——需引擎结算点扩展，另立工作项（决策 19）。
4. （粒子增强见 AURA-07，可选可砍。）
