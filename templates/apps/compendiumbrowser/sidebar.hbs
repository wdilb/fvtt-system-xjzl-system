<div class="xjzl-sidebar-content">
    <div class="xjzl-cb-search-box">
        <i class="fas fa-search"></i>
        <input type="text" name="search" value="{{searchQuery}}" placeholder="搜索名称..." autocomplete="off">
        {{#if searchQuery}}
        <a class="xjzl-cb-search-clear" data-action="resetFilters" title="清除"><i class="fas fa-times"></i></a>
        {{/if}}
    </div>

    <div class="xjzl-cb-filter-header">
        <span><i class="fas fa-filter"></i> 筛选</span>
        <a class="reset-link" data-action="resetFilters">重置</a>
    </div>

    <div class="xjzl-cb-filters-list">
        {{#each filterList}}
        <details class="filter-group xjzl-filter-panel" {{#if this.isOpen}}open{{/if}}>
            <summary class="filter-title">{{this.label}}</summary>
            <div class="filter-options">
                {{#each this.options}}
                <label class="checkbox-label {{#if this.checked}}checked{{/if}}" role="checkbox" aria-checked="{{this.checked}}">
                    <input type="checkbox" class="xjzl-filter-checkbox" data-filter="{{../key}}" value="{{this.val}}"
                        {{checked this.checked}}>
                    {{this.label}}<span class="cb-count {{#if this.isZero}}is-zero{{/if}}">({{this.count}})</span>
                </label>
                {{/each}}
            </div>
        </details>
        {{else}}
        <p class="no-filters">当前分类无高级筛选</p>
        {{/each}}
    </div>

    <div class="xjzl-cb-sidebar-footer">
        <button type="button" class="random-btn" data-action="randomize">
            <i class="fas fa-dice-d20"></i> 随机抽取
        </button>
    </div>
</div>