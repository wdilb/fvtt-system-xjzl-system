<div class="compendium-browser-content">
    <header class="browser-header">
        <h1>江湖万卷阁</h1>
        <button type="button" data-action="refresh">
            <i class="fas fa-sync"></i> 刷新索引
        </button>
    </header>

    <div class="browser-body">
        {{#if isLoaded}}
            <div class="status-panel">
                <p>索引已就绪。当前收录：</p>
                <ul>
                    {{#each counts as |count type|}}
                        <li><strong>{{type}}</strong>: {{count}} 件</li>
                    {{/each}}
                </ul>
                <p>（筛选界面将在第二阶段开发）</p>
            </div>
        {{else}}
            <div class="loading">
                <i class="fas fa-spinner fa-spin"></i> 正在读取江湖卷宗...
            </div>
        {{/if}}
    </div>
</div>