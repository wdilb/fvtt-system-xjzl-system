<div class="standard-form">
    <!-- 名称 -->
    <div class="form-group">
        <label>显示名称</label>
        <div class="form-fields">
            <input type="text" name="label" value="{{label}}" placeholder="例如：烈火燎原" />
        </div>
    </div>

    <!-- 范围 -->
    <div class="form-group">
        <label>半径范围 (格)</label>
        <div class="form-fields">
            <input type="number" name="distance" value="{{distance}}" step="1" min="1" />
        </div>
    </div>

    <!-- 颜色 -->
    <div class="form-group">
        <label>显示颜色</label>
        <div class="form-fields">
            <input type="color" name="color" value="{{color}}"
                style="height: 32px; width: 100%; border:none; padding:0; background:none; cursor:pointer;" />
        </div>
    </div>

    <hr style="border-color: #444; margin: 10px 0;">

    <!-- 模式选择 -->
    <div class="form-group">
        <label>放置模式</label>
        <div class="form-fields">
            <select name="mode" id="aoe-mode-select">
                {{selectOptions modes selected=defaultMode}}
            </select>
        </div>
    </div>

    <!-- Token 选择 (下拉列表) -->
    <!-- 我们给它加个 ID，如果你想写 JS 做显隐切换也可以，不写也无所谓，反正后台逻辑会判断 -->
    <div class="form-group" id="token-select-group">
        <label>目标 Token</label>
        <div class="form-fields">
            <select name="tokenId">
                {{selectOptions allTokens selected=selectedTokenId valueAttr="id" labelAttr="name"}}
            </select>
        </div>
    </div>

    <p class="notes" style="margin-top: 5px; font-size: 0.8em; color: #888;">
        <i class="fas fa-info-circle"></i> 静态模式将生成在屏幕中心，生成后可拖动。
    </p>

    <!-- 底部按钮 -->
    <footer class="form-footer">
        <button type="button" data-action="create" class="save-button">
            <i class="fas fa-check"></i> 生成区域
        </button>
    </footer>
</div>