<div class="wizard-main-wrapper">
    {{!-- 头部区域 --}}
    <div class="wizard-header">
        <div class="header-deco-line"></div>
        <h3>{{actor.name}} <span class="step-indicator">- 步 骤 {{data.currentStep}}</span></h3>
    </div>

    {{!-- 主体内容区 --}}
    <div class="wizard-step-body scrollable">

        {{#if (eq data.currentStep 1)}}
        {{!-- 新增：Step 1 专属背景图 --}}
        <div class="step-bg" style="background-image: url('systems/xjzl-system/assets/picture/wizard-step1-bg.png');">
        </div>

        <div class="wizard-form-container relative-z">
            <div class="form-group split-group">
                <div class="sub-group">
                    <label>角色姓名</label>
                    <div class="form-fields">
                        <input type="text" name="info.name" value="{{data.info.name}}" autofocus placeholder="请输入尊姓大名">
                    </div>
                </div>
                {{!-- 新增：字 --}}
                <div class="sub-group">
                    <label>字 (选填)</label>
                    <div class="form-fields">
                        <input type="text" name="info.zi" value="{{data.info.zi}}" placeholder="如：玄德、太白">
                    </div>
                </div>
            </div>

            <div class="form-group">
                <label>江湖称号 (选填)</label>
                <div class="form-fields">
                    <input type="text" name="info.title" value="{{data.info.title}}" placeholder="如：天下第一">
                </div>
            </div>

            <div class="form-group split-group">
                <div class="sub-group">
                    <label>性别</label>
                    <div class="form-fields">
                        <select name="info.gender">
                            {{selectOptions choices.genders selected=data.info.gender localize=true}}
                        </select>
                    </div>
                </div>
                {{!-- 进阶：门派选择卡片 --}}
                <div class="sub-group">
                    <label>所属门派</label>
                    <div class="form-fields">
                        <!-- 隐藏的 input 用来被 formData 抓取 -->
                        <input type="hidden" name="info.sect" value="{{data.info.sect}}">
                        <!-- 伪装成按钮的显示框 -->
                        <div class="sect-selector-btn" data-action="openSectSelector">
                            <span class="sect-name">{{localize (lookup choices.sects data.info.sect)}}</span>
                            <span class="sect-hint">更改门派 <i class="fas fa-exchange-alt"></i></span>
                        </div>
                    </div>
                </div>
            </div>

            <div class="form-group">
                <label>初始银两</label>
                <div class="form-fields">
                    <div class="silver-input-wrap">
                        <i class="fas fa-coins"></i>
                        <input type="number" name="info.silver" value="{{data.info.silver}}" min="0">
                    </div>
                </div>
            </div>
        </div>
        {{/if}}

        {{!-- ================= Step 2: 命理性格 ================= --}}
        {{#if (eq data.currentStep 2)}}
        <div class="step-bg" style="background-image: url('systems/xjzl-system/assets/picture/wizard-step2-bg.png');">
        </div>
        <div class="wizard-form-container relative-z origin-step-container">

            <div class="origin-split-layout">

                {{!-- 左半区：身世背景 --}}
                <div class="origin-column">
                    <h3 class="column-title"><i class="fas fa-scroll"></i> 身世背景</h3>

                    <div class="origin-inner-split">
                        {{!-- 左侧：滚动列表 --}}
                        <div class="origin-list-sidebar scrollable">
                            {{#each originsPack.backgrounds as |bg|}}
                            <div class="origin-list-item {{#if (eq ../data.origins.background.uuid bg.uuid)}}selected{{/if}}"
                                data-action="selectOriginItem" data-type="background" data-uuid="{{bg.uuid}}">
                                <img src="{{bg.img}}">
                                <span>{{bg.name}}</span>
                            </div>
                            {{/each}}
                        </div>

                        {{!-- 右侧：详情展示区 (被 JS 动态替换) --}}
                        <div class="origin-detail-view scrollable">
                            {{#if data.origins.background}}
                            <div class="detail-header">
                                <img src="{{data.origins.background.img}}" class="detail-icon">
                                <h4 class="detail-name">{{data.origins.background.name}}</h4>
                            </div>
                            <div class="detail-desc">{{{data.origins.background.system.description}}}</div>
                            <div class="detail-assets">
                                <strong><i class="fas fa-box-open"></i> 初始行囊：</strong>
                                {{{data.origins.background.system.assets}}}
                            </div>
                            {{else}}
                            <div class="origin-empty-box"><i class="fas fa-arrow-left"></i> 请从左侧选择身世背景</div>
                            {{/if}}
                        </div>
                    </div>
                    {{!-- 背景特有的提示 --}}
                    <div class="system-warning-note">
                        <i class="fas fa-info-circle"></i> 提示：背景附带的初始行囊物品，请在建卡完成后，自行从江湖万卷阁拖入角色卡。
                    </div>
                </div>

                {{!-- 右半区：命理性格 --}}
                <div class="origin-column">
                    <h3 class="column-title"><i class="fas fa-theater-masks"></i> 命理性格</h3>

                    <div class="origin-inner-split">
                        {{!-- 左侧：滚动列表 --}}
                        <div class="origin-list-sidebar scrollable">
                            {{#each originsPack.personalities as |pers|}}
                            <div class="origin-list-item {{#if (eq ../data.origins.personality.uuid pers.uuid)}}selected{{/if}}"
                                data-action="selectOriginItem" data-type="personality" data-uuid="{{pers.uuid}}">
                                <img src="{{pers.img}}">
                                <span>{{pers.name}}</span>
                            </div>
                            {{/each}}
                        </div>

                        {{!-- 右侧：详情展示区 (被 JS 动态替换) --}}
                        <div class="origin-detail-view scrollable">
                            {{#if data.origins.personality}}
                            <div class="detail-header">
                                <img src="{{data.origins.personality.img}}" class="detail-icon">
                                <h4 class="detail-name">{{data.origins.personality.name}}</h4>
                            </div>
                            <div class="detail-desc">{{{data.origins.personality.system.description}}}</div>

                            {{#if data.origins.personality.system.options.length}}
                            <div class="personality-skills">
                                <label class="skill-prompt"><i class="fas fa-hand-sparkles"></i> 请选择 2 项性格专长 (每项
                                    +2)：</label>
                                <div class="skill-checkbox-group">
                                    {{#each data.origins.personality.system.options as |opt|}}
                                    <label class="skill-checkbox">
                                        <input type="checkbox" value="{{opt}}" data-action="togglePersonalitySkill"
                                            {{checked (includes ../data.origins.personality.system.chosen opt)}}>
                                        <span>{{localize (concat "XJZL.Skills." (capitalize opt))}}</span>
                                    </label>
                                    {{/each}}
                                </div>
                            </div>
                            {{/if}}
                            {{else}}
                            <div class="origin-empty-box"><i class="fas fa-arrow-left"></i> 请从左侧选择命理性格</div>
                            {{/if}}
                        </div>
                    </div>
                </div>

            </div>
        </div>
        {{/if}}

        {{!-- ================= Step 3: 处世结交 ================= --}}
        {{#if (eq data.currentStep 3)}}
        <div class="step-bg" style="background-image: url('systems/xjzl-system/assets/picture/wizard-step3-bg.png');">
        </div>
        <div class="wizard-form-container relative-z social-step-container">

            <div class="social-grid">
                {{!-- 基础生理与嗜好 --}}
                <div class="social-panel">
                    <div class="panel-header">形貌与嗜好</div>
                    <div class="form-group split-group">
                        <div class="sub-group">
                            <label>年龄</label>
                            <div class="form-fields"><input type="number" name="info.age" value="{{data.info.age}}">
                            </div>
                        </div>
                        <div class="sub-group">
                            <label>身高</label>
                            <div class="form-fields"><input type="text" name="info.height" value="{{data.info.height}}"
                                    placeholder="如: 八尺"></div>
                        </div>
                        <div class="sub-group">
                            <label>体重</label>
                            <div class="form-fields"><input type="text" name="info.weight" value="{{data.info.weight}}"
                                    placeholder="如: 匀称"></div>
                        </div>
                    </div>
                    <div class="form-group">
                        <label>个人嗜好 (选填3项)</label>
                        <div class="hobbies-row">
                            {{#each hobbySlots as |slot|}}
                            <select name="social.shihao.{{slot.index}}">
                                {{selectOptions @root.choices.hobbies selected=slot.value blank="-" localize=true}}
                            </select>
                            {{/each}}
                        </div>
                    </div>
                </div>

                {{!-- 声望与业障 --}}
                <div class="social-panel">
                    <div class="panel-header">声望与业障</div>
                    <div class="reputation-grid">
                        <div class="rep-item">
                            <label>朝廷声望</label>
                            <input type="number" name="social.rep_chaoting" value="{{data.social.rep_chaoting}}">
                            <select name="social.attitude_chaoting">
                                {{selectOptions choices.attitudes selected=data.social.attitude_chaoting localize=true}}
                            </select>
                        </div>
                        <div class="rep-item">
                            <label>武林声望</label>
                            <input type="number" name="social.rep_wulin" value="{{data.social.rep_wulin}}">
                            <select name="social.attitude_wulin">
                                {{selectOptions choices.attitudes selected=data.social.attitude_wulin localize=true}}
                            </select>
                        </div>
                        <div class="rep-item">
                            <label>世俗态度</label>
                            <select name="social.attitude_shisu" style="margin-top:auto;">
                                {{selectOptions choices.attitudes selected=data.social.attitude_shisu localize=true}}
                            </select>
                        </div>
                    </div>
                </div>
            </div>

            {{!-- 人物传记 --}}
            <div class="social-panel" style="margin-top: 20px;">
                <div class="panel-header">生平传记</div>
                <div class="bio-prosemirror-wrapper">
                    <prose-mirror name="info.bio" value="{{data.info.bio}}" toggled="true">
                        {{{data.info.bio}}}
                    </prose-mirror>
                </div>
            </div>

            {{!-- 初始关系网 --}}
            <div class="social-panel" style="margin-top: 20px;">
                <div class="panel-header flex-between">
                    <span>初始关系网</span>
                    <button type="button" class="btn-small-add" data-action="addRelation"><i class="fas fa-plus"></i>
                        新增结识</button>
                </div>

                <div class="relations-container">
                    {{#if data.social.relations.length}}
                    {{#each data.social.relations as |rel i|}}
                    <div class="relation-row">
                        <input type="text" name="social.relations.{{i}}.name" value="{{rel.name}}" placeholder="NPC姓名">
                        <input type="text" name="social.relations.{{i}}.type" value="{{rel.type}}"
                            placeholder="关系(恩人/仇家等)">
                        <input type="number" name="social.relations.{{i}}.value" value="{{rel.value}}"
                            placeholder="好感度">
                        <button type="button" data-action="deleteRelation" data-index="{{i}}" class="btn-delete-rel"><i
                                class="fas fa-trash"></i></button>
                    </div>
                    {{/each}}
                    {{else}}
                    <div class="empty-hint">孤身一人，暂无牵绊</div>
                    {{/if}}
                </div>
            </div>

        </div>
        {{/if}}
        {{#if (eq data.currentStep 4)}}<div class="placeholder-text">资源配置：运算引擎加载中...</div>{{/if}}
        {{#if (eq data.currentStep 5)}}<div class="placeholder-text">技艺精通：面板组装中...</div>{{/if}}

        {{#if (eq data.currentStep 6)}}
        <div class="wizard-final-view">
            <img class="final-bg" src="systems/xjzl-system/assets/picture/wizard-birth-bg.png">
            <div class="final-content">
                <i class="fas fa-yin-yang fa-4x yin-yang-spin"></i>
                <h2>诸事具备，只欠东风</h2>
                <p>
                    天地洪荒，命运已定。<br>
                    点击“完成建卡”后，旧的躯壳将消散，<br>
                    你所选择的武学、修为与天赋将注入灵魂。
                </p>
                <div class="warning-text">
                    <i class="fas fa-exclamation-triangle"></i> 此操作不可逆转
                </div>
            </div>
        </div>
        {{/if}}

    </div>
</div>