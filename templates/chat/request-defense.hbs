<div class="xjzl-chat-card request-card">

    {{!-- 1. 头部 (完全复用 move-card 结构) --}}
    <header class="card-header"
        style="border-bottom: 2px solid var(--xjzl-gold); padding-bottom: 5px; margin-bottom: 10px; display: flex; align-items: center; gap: 10px;">

        {{!-- 使用 Token 头像 --}}
        <img src="{{targetImg}}" width="40" height="40"
            style="flex: 0 0 40px; border:1px solid #333; background:#000; border-radius: 4px; object-fit: cover;" />

        <div style="flex: 1;">
            <h3 style="margin: 0; font-weight: 900; border: none; color: #8b0000; font-size: 1.2em;">虚招对抗检定</h3>
            <div style="font-size: 0.8em; color: #555;">
                <span style="background: #483d8b; color: #fff; padding: 0 4px; border-radius: 2px;">对抗</span>
                - {{targetName}}
            </div>
        </div>
    </header>

    {{!-- 2. 描述区域 --}}
    <div class="card-description"
        style="font-style: italic; font-size: 0.9em; color: #444; margin-bottom: 10px; padding: 5px; background: rgba(0,0,0,0.03); border-radius: 4px;">
        <p style="margin:0;">
            <b>{{attackerName}}</b> 的虚招已命中！<br>
            {{targetName}} 的架招面临威胁，需进行 [看破] 检定。
        </p>
    </div>

    {{!-- 3. 数值展示 (复用 card-stats 样式) --}}
    <div class="card-stats"
        style="display: flex; justify-content: space-around; align-items: center; margin-bottom: 15px; background: #fff; border: 1px dashed #ccc; padding: 10px; border-radius: 4px;">

        {{!-- 攻方强度 --}}
        <div style="text-align: center;">
            <div style="font-size: 0.8em; color: #666;">虚招值 (DC)</div>
            <div style="font-size: 1.8em; font-weight: 900; color: #b22222; line-height: 1;">
                {{attackTotal}}
            </div>
            <div style="font-size: 0.7em; color: #888; margin-top:2px;">
                1d20({{{rollTotal}}}) + {{feintVal}}
            </div>
        </div>

    </div>

    {{!-- 4. 按钮区域 --}}
    <div class="card-buttons" style="display: flex; flex-direction: column; gap: 5px;">
        <button data-action="rollDefendFeint" data-target-uuid="{{targetUuid}}" data-attack-total="{{attackTotal}}"
            data-origin-msg-id="{{originMessageId}}" style="background: #483d8b; color: white;">
            <i class="fas fa-eye"></i> 进行看破检定
        </button>
    </div>
</div>