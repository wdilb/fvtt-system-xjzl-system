<div class="xjzl-chat-card death-card">
    {{#if isDead}}
    {{!-- 1. 死亡状态 --}}
    <header class="card-header" style="border-left: 4px solid #000;">
        <i class="fas fa-skull" style="font-size: 2em; color: #333; margin-right: 10px;"></i>
        <h3 style="color: #000;">{{localize "XJZL.Status.Dead"}}</h3>
    </header>

    <div class="card-content-wrapper">
        <!-- 这里的说明文字原本就在，保持不变 -->
        <div class="card-description">{{localize "XJZL.UI.Chat.DeathCard.DeadFlavor"}}</div>

        <button data-action="rollDeathSave" class="btn-effect" style="background:#333 !important;">
            <i class="fas fa-dice-d20"></i> {{localize "XJZL.UI.Chat.DeathCard.DeathSave"}} (DC 10)
        </button>
    </div>

    {{else}}

    {{!-- 2. 濒死状态 --}}
    <header class="card-header" style="border-left: 4px solid #8b0000;">
        <i class="fas fa-procedures" style="font-size: 2em; color: #8b0000; margin-right: 10px;"></i>
        <h3 style="color: #8b0000;">{{localize "XJZL.Status.Dying"}}</h3>
    </header>

    <div class="card-content-wrapper">
        {{!-- 修复：将说明文字移出了 if/else 判断，确保它像原来一样一直显示 --}}
        <div class="card-description">{{localize "XJZL.UI.Chat.DeathCard.DyingFlavor"}}</div>

        {{!-- 投掷结果展示 (AI 优化的部分保留) --}}
        {{#if rollResult}}
        <div class="disability-result">
            <div class="roll-val">{{rollResult.total}}</div>
            <div class="roll-formula">(d100)</div>
        </div>

        <div
            style="background:rgba(139,0,0,0.05); padding:10px; border-radius:4px; margin-bottom:15px; border-left:3px solid #8b0000;">
            <div style="font-weight:bold; color:#8b0000; margin-bottom:5px;">{{rollResult.label}}</div>
            <div style="font-size:0.9em; color:#333; line-height:1.5;">{{rollResult.description}}</div>
        </div>
        {{/if}}

        <button data-action="rollDisability" class="btn-damage">
            <i class="fas fa-crutch"></i> {{localize "XJZL.UI.Chat.DeathCard.RollDisability"}} (d100)
        </button>
    </div>
    {{/if}}
</div>