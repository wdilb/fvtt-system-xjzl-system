<section class="xjzl-details-container flexcol">
    {{!-- 故事描述 --}}
    <div class="xjzl-section">
        <div class="xjzl-section-header"><i class="fas fa-scroll"></i> {{localize "XJZL.Background.Description"}}</div>
        {{editor system.description target="system.description" button=true owner=owner editable=editable}}
    </div>

    {{!-- 初始行囊/预设物资 --}}
    <div class="xjzl-section">
        <div class="xjzl-section-header"><i class="fas fa-suitcase"></i> {{localize "XJZL.Background.Assets"}}</div>
        {{!-- 这里目前只显示描述文字，未来可以扩展为存放物品引用的区域 --}}
        <div class="xjzl-info-box">
            {{#if system.items.length}}
            {{!-- 预留：如果未来有物品引用，在这里渲染列表 --}}
            {{/if}}
            <p class="xjzl-hint">此背景建议初始携带以下物资（请手动从合集包拖拽）：</p>
            <div class="xjzl-assets-text">
                {{#if system.assets}}
                {{{system.assets}}}
                {{else}}
                <span class="xjzl-empty">暂无初始物资记录。</span>
                {{/if}}
            </div>
        </div>
    </div>
</section>