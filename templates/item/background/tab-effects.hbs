<section class="tab scrollable {{#if (eq tabs.primary 'effects')}}active{{/if}}" data-tab="effects"
    data-group="primary">
    <div class="xjzl-flex-col">
        <p class="xjzl-hint-box"
            style="font-size:0.85em; background: rgba(0,0,0,0.03); padding: 10px; border-radius: 4px; margin-bottom: 15px;">
            <i class="fas fa-info-circle"></i> 在此配置身世带来的属性或技能加成（如：力量+5）。<br>
            <b>注意：</b> 必须开启“被动 (Transfer)”选项加成才会生效。
        </p>

        <div class="xjzl-effects-list">
            {{#each effects as |effect|}}
            <div class="xjzl-card xjzl-flex-row"
                style="align-items: center; padding: 8px; margin-bottom: 5px; border: 1px solid #1a3c4033; background: rgba(255,255,255,0.3);">
                <img src="{{effect.img}}" width="32" height="32" style="border:1px solid #1a3c40;" />
                <div class="xjzl-flex-col xjzl-grow" style="margin-left: 10px;">
                    <span
                        style="font-weight:bold; {{#if effect.disabled}}color:#999; text-decoration:line-through;{{/if}}">{{effect.name}}</span>
                </div>
                <div class="xjzl-flex-row" style="gap: 10px;">
                    <a data-action="toggleEffect" data-id="{{effect.id}}" title="启用/禁用"><i
                            class="fas {{#if effect.disabled}}fa-toggle-off{{else}}fa-toggle-on{{/if}}"></i></a>
                    <a data-action="editEffect" data-id="{{effect.id}}" title="编辑"><i class="fas fa-edit"></i></a>
                    <a data-action="deleteEffect" data-id="{{effect.id}}" title="删除"><i class="fas fa-trash"></i></a>
                </div>
            </div>
            {{else}}
            <div class="xjzl-empty-hint"
                style="text-align:center; padding: 20px; border: 1px dashed #1a3c40; color: #1a3c40;">
                暂无数值加成。请点击下方按钮新建。
            </div>
            {{/each}}
        </div>

        <button type="button" data-action="createEffect" style="margin-top:15px; background: #1a3c40; color: white;">
            <i class="fas fa-plus"></i> 新健身世数值加成
        </button>
    </div>
</section>